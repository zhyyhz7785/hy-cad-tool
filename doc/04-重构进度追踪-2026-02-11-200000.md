# HyCADTool 重构进度追踪

> **版本**：v1.0  
> **日期**：2026-02-11  
> **目的**：精确记录旧→新命令迁移进度，方便随时续接重构

---

## 一、总览

| 指标 | 数值 |
|---|---|
| 旧项目活跃命令 | ~67 个 |
| 已迁移到 Refactored | 40 个命令文件（含 Test/Base/Paper） |
| 核心业务命令已迁移 | 31 个 |
| 待迁移 | ~30 个（含低优先级） |
| Refactored 总文件 | 280 个 .cs |
| 架构合规 | ✅ 依赖方向正确，Domain 纯净 |

---

## 二、已完成模块（按时间倒序）

### 2.1 地脚螺栓 hyab*（2026-02-11）✅

| 新命令 | 别名 | 旧文件 | 状态 |
|---|---|---|---|
| `AnchorBoltCommand` | hyab | `Hyab_AnchorBolt/hyab.cs` | ✅ |
| `AnchorBoltAlignCommand` | hyabA | `hyabA_Align.cs` | ✅ |
| `AnchorBoltCalcCommand` | hyabC | `hyabC_Calculat.cs` | ✅ |
| `AnchorBoltToggleDisplayCommand` | hyabCD | `hyabCD_ChangeDisPlay.cs` | ✅ |
| `AnchorBoltTableCommand` | hyabCT | `hyabCT_CreateAnchorBoltTable.cs` | ✅ |
| `AnchorBoltSectionCommand` | hyabR | `hyabR_RectSection.cs` | ✅ |

新增支撑文件：`AnchorBolt.cs`（Domain）、`ExtensionDictionaryService.cs`（Infrastructure）

### 2.2 标高命令 hybg*（2026-02-11）✅

| 新命令 | 别名 | 旧文件 | 状态 |
|---|---|---|---|
| `DrawElevationCommand` | bg | `hybg/hybg.cs` | ✅ |
| `UpdateElevationTextCommand` | bgu | `hybgu_UpdateElevationText.cs` | ✅ |
| `RotateElevationCommand` | bgR | `hybgR_RotateElevation.cs` | ✅ |
| `TextsCreateElevationCommand` | hybgTCE | `hybgTCE_TextsCreatElevation.cs` | ✅ |

新增支撑文件：`ElevationSymbolJig.cs`（Interactive）、`ElevationService.cs`（Infrastructure）、`ElevationSymbolState.cs`（Domain/Enums）

### 2.3 尺寸标注 hydim* 独立命令（2026-02-11）✅

| 新命令 | 别名 | 旧文件 | 状态 |
|---|---|---|---|
| `AddVertexAtIntersectionsCommand` | hydimA | `hydimA_AddVertexAtIntersections.cs` | ✅ |
| `SplitDimensionCommand` | sd | `hydimS_SplitDimensionAtLine.cs` | ✅ |

### 2.4 底板配筋 BaseRein（2026-02 早期）✅

| 组件 | 说明 | 状态 |
|---|---|---|
| `BaseReinforcementConfig` | 完整配置模型 | ✅ |
| `BaseReinPanel.xaml` + `BaseReinPanelViewModel` | 完整面板 | ✅ |
| `BaseReinforcementService` Step 1 | 整理底图 | ✅ |
| `BaseReinforcementService` Step 2 | 选择删除文字 | ✅ |
| `BaseReinforcementService` Step 4 | 生成配筋面积 | ✅ |
| `BaseReinforcementService` Step 5 | 绘制钢筋 | ✅ |
| `BaseReinforcementService` Step 6 | 标注配筋区域 | ✅ |

### 2.5 钢筋命令（2026-01～02）✅

| 新命令 | 别名 | 旧命令 | 状态 |
|---|---|---|---|
| `DrawReinforcementCommand` | gj | gj | ✅ |
| `DrawOffsetPolylineCommand` | gg | gg | ✅ |
| `ReinAddAnchorCommand` | g1/g2 | g1/g2 | ✅ |
| `ReinCutCommand` | gd | gd | ✅ |
| `ReinExtendCommand` | ge | ge | ✅ |
| `ReinQuickExtendCommand` | ge1 | ge1 | ✅ |
| `MleaderReinCommand` | gb/gb1/gb2 | gb/gb1/gb2 | ✅ |

支撑服务：`ReinService`（除 `DimensionRein` 外完成）

### 2.6 HY 设置面板 ✅

`SettingsPanel` + `SettingsPanelViewModel` + `StyleService` + `TextBoxHelper`

### 2.7 早期已迁移命令 ✅

| 新命令 | 别名 | 说明 |
|---|---|---|
| `BreakCurvesCommand` | HYBC | 曲线打断（性能提升 70-90%） |
| `OverKillCommand` | HYOV | 线重叠消除 |
| `JoinParallelLinesCommand` | HYJP | 平行线连接 |
| `DCELCommand` | HYDCEL | DCEL 构建 |
| `MBRCommand` | HYMBR | 最小外接矩形聚类 |
| `DrawPilesCommand` | — | 桩基绘制（面板调用） |
| `PileVoronoiOptimizationCommand` | — | 桩基 Voronoi 优化 |
| `GroupCirclesByElevationCommand` | — | 按标高分组圆 |
| `Elevation3DCommand` | HY3 | 3D 标高 |
| `SurfaceBasedElevation3DCommand` | C15 | 曲面标高 3D |
| `LocateTifCommand` | HYLOCATETIF | 定位 TIF |
| Paper 命令 | ph/phh/pv/pvv | 图纸排版（4 个） |
| `ShowPanelCommand` | — | 面板入口 |
| `FilterPanel` 相关 | — | 选择过滤面板 |

---

## 三、待迁移模块（按优先级排序）

### 3.1 🔴 P0 — 尺寸标注 DimensionForReinforcement

| 旧命令 | 旧文件 | 行数 | 说明 |
|---|---|---|---|
| `dds` | `HyDim/hydims.cs` | ~884 | 单个配筋尺寸标注 |
| `ddss` | `HyDim/hydims.cs` | 同上 | 批量配筋尺寸标注 |

**难度**：高（依赖链深，884+769 行，需单独一轮）  
**依赖**：ReinService.DimensionRein（当前未完成的部分）

### 3.2 🟡 P1 — 设备基础 hyef*

| 旧命令 | 旧文件 | 说明 |
|---|---|---|
| `hyef_Base_ConstructBaseData` | `EF_Commands.cs` | 构造基础数据 |
| `hyef_Base_HighlightBoltData` | `EF_Commands.cs` | 高亮螺栓数据 |
| `hyef_Axis_Construct` | `EF_Commands.cs` | 构造轴线 |
| `hyef_Axis_Initialize` | `EF_Commands.cs` | 初始化轴线 |
| `hyef_Axis_Display` | `EF_Commands.cs` | 显示轴线结构 |
| `hyef_Axis_CreateTable` | `EF_Commands.cs` | 创建轴线表格 |

**难度**：中（6 命令在同一文件，独立模块）

### 3.3 🟡 P1 — 导出工具 hyExport*

| 旧命令 | 说明 |
|---|---|
| `ExportEntityPropertiesToCsv` | 导出实体属性到 CSV |
| `hyex_MarkdownTable` | 导出为 Markdown 表格 |
| `hydl_DrawLinesOnEachLayer` | 每图层画线 |

**难度**：低

### 3.4 🟡 P2 — 轴线 / 筏板 / 选择

| 旧命令 | 说明 |
|---|---|
| `hyAxis_AlignedAxisText` | 轴线对齐文字 |
| `HyRT_aftThicknessText` | 筏板厚度文字 |
| `hysrt` | 选择钢筋文字 |

**难度**：低

### 3.5 🟢 P2 — 块操作 / 替换

| 旧命令 | 说明 |
|---|---|
| `HYc2_block2leader` | 块转引线 |
| `HYc2_block_color` | 块图层颜色 |
| `abrc` | 多边形交集替换 |
| `abrcs` | 批量多边形交集替换 |

**难度**：低-中

### 3.6 🟢 P3 — 3D / 剖面

| 旧命令 | 说明 |
|---|---|
| `hy3_Generate` | 3D 生成 |
| `hy3C_CreateSection` | 创建剖面 |

**难度**：中

### 3.7 🟢 P3 — 其他 / 低频

| 旧命令 | 说明 | 备注 |
|---|---|---|
| `HYBL` | 断线 | 可能与 HYBC 合并 |
| `ggj` (ReinOutside) | 外侧配筋 | 钢筋系列补充 |
| `d2p` | DCEL 转多段线 | 已有 DCELCommand |
| `hyDcP_Poly` / `HyDcL_Line` | 垫层绘制 | 低频 |
| `DetectClosedRegions` | 检测封闭区域 | 实验性 |
| `GPT_ADAPTIVE_GRID` | 自适应网格 | 实验性 |
| MBR 布局系列 | 视口/图框 | 低频 |
| 样式设置系列 | 设置文字/标注/引线样式 | 已由 StyleService 覆盖 |
| DimPoint 系列 | 标注点 | 实验性 |
| TEST_* | 测试命令 | 无需迁移 |

---

## 四、当前 TestCommand.cs 状态

```csharp
// 无 PendingCommand 时执行：
new AnchorBoltSectionCommand().Execute();
```

---

## 五、下一步建议

| 顺序 | 任务 | 预估 | 说明 |
|---|---|---|---|
| 1 | **DimensionForReinforcement (dds/ddss)** | 1-2 轮 | 最大遗留项，完成后钢筋全链路打通 |
| 2 | **设备基础 hyef*** | 1 轮 | 独立模块，6 命令集中在一个文件 |
| 3 | **导出工具** | 半轮 | 简单，实用性高 |
| 4 | **ggj (ReinOutside)** | 半轮 | 钢筋系列最后补充 |
| 5 | **其余杂项** | 按需 | 优先级低，按实际使用频率决定 |

---

## 六、架构备忘

```
Domain（118 文件）  →  平台无关，可迁 Blender
    ├── ValueObjects/Geometry    20+ 几何类型
    ├── Services/GeometryAlgorithms  16 算法服务
    ├── DataStructures/DCEL     4 文件
    └── Interfaces              11 服务接口

Infrastructure（92 文件）  →  AutoCAD 封装
    ├── AutoCAD/Services        21 服务实现
    ├── AutoCAD/Extensions      9 扩展方法
    ├── AutoCAD/Selection       12 选择过滤
    └── Configuration           7 配置加载

Presentation（66 文件）  →  命令与 UI
    ├── Commands                40 命令
    ├── ViewModels              6 视图模型
    └── Views                   6 面板
```

### 关键替换规则（迁移时参考）

| 旧代码 | 新代码 |
|---|---|
| `ReinPanel.ActivePanel?.xxx` | `SettingsPanelViewModel.Current?.xxx` |
| `BaseConfig.xxx` | 从 SettingsPanelViewModel 读取 |
| `ZTools.CreateSolidCircle` | `EntityExtensions.CreateSolidCircle` |
| `.ToSpace()` | `EntityExtensions.ToSpace()` |
| `ZTools.AddMleader` | `MLeaderExtensions.AddMleader` |
| `[CommandMethod]` | 不用，通过 TestCommand 调用 |

---

**文档版本**: v1.0 | **生成**: 2026-02-11
