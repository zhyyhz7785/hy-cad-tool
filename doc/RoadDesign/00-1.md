rCs

需要深度优化我的wpf面板

首先是各种类型的断面的创建，第一层       有机动车道    非机动车道  绿化带 分隔带 

第二层 机动车  非机动车道 隔离带 道下面 有 面层 基层 垫层  绿化带没有第二层
第三层 面层  有三层 可以两层  基层 可以1~5层 垫层 1~2 层
我需要一个属性面板，可以迅速输入以上信息创建模型数据，还需要有一个从AutoCAD图形中选取图形确定模型参数的命令
左侧大纲面板 是放置断面的容器，当选中区域处于当前时，激活属性面板，属性修改值同步传递到 大纲面板存储，大纲面板中可以有宽度的输入，默认米（你可以考虑优化）
分为3部分，1 中央隔离带 宽度 是否有的checkbox
                      2 左侧，开始的时候可以确定板块数量（）
			从中央开始，到左侧边界顺序（**e**右=(-1,0)）  机动车道1（可以切换模块） 宽度 是否有的checkbox 
											机动车道2 （可以切换模块）宽度 是否有的checkbox
											机动车道3（可以切换模块） 宽度 是否有的checkbox

​											绿化带 （可以切换模块）  宽度 是否有的checkbox

​											非机动车道（可以切换模块）  宽度 是否有的checkbox
​											人行道（可以切换模块）  宽度 是否有的checkbox
​											绿化带（可以切换模块）  宽度 是否有的checkbox
​                      3 右侧  开始的时候可以确定板块数量（）
​                                     从中央开始，到右侧边界顺序**e**右=(1,0) 机动车道1（可以切换模块） 宽度 是否有的checkbox 
​											机动车道2 （可以切换模块）宽度 是否有的checkbox
​											机动车道3（可以切换模块） 宽度 是否有的checkbox

​											绿化带 （可以切换模块）  宽度 是否有的checkbox

​											非机动车道（可以切换模块）  宽度 是否有的checkbox
​											人行道（可以切换模块）  宽度 是否有的checkbox
​											绿化带（可以切换模块）  宽度 是否有的checkbox

参考Blender类似功能，先帮我优化左侧大纲面板，右侧属性面板

实时预览窗口 不要