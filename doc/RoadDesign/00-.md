

# 指定 ProxyIP（不要同时写 wk）
/?ed=2048&p=1.1.1.1
/?ed=2048&p=proxy.example.com:443
/?ed=2048&p=[2001:db8::1]:443

# 指定地区（让 Worker 自动选该地区的 ProxyIP）
/?ed=2048&wk=jp
/?ed=2048&wk=sg&rm=no

# 指定 SOCKS5（可与 wk 搭配）
/?ed=2048&s=user:pass@socks5.host:1080&wk=us

我需要制定一个详细的计划，完成建模和在AutoCAD中绘图


规划道路计用的实体类型（现实世界存在，且需要真实模拟，在AutoCAD中平面显示，Blender中3d显示的 实体）

 控制类型 点  线 面（现实世界没有单独的面，物体总是有厚度的，在这里我想用它表达控制符号的概念，比如参考面 和选中面 ，点）

如何在AutoCAD中把实体 控制体 和AutoCAD中的显示紧密结合 ，存储的

比如 Alignment 基本由Polyline构成显示 ，应该能够方便的的存储 和在AutoCAD中重绘，所有的需要在AutoCAD中绘制的类型都需要这样

类似鸿业里面的概念，线型 道路 交通 绿化 只要制作了，都有记录类似图1，实体的编辑都会存储，方便回退。和AutoCAD的图形 ctrl+z Ctrl+y两套系统

道路是由 绿化带 人行道 非机动车道 分隔带 机动车道 中央分隔带 自由断面 等多种构件组成。他们的生成方式 由横断面延伸构成，横断面图（结构详图）上面图2 是道路和人行路的图纸。

我的构想
**建模**

1 完成路线建模，路线建模后，需要随时能够添加记录的路线 在AutoCAD中，在AutoCAD中以Polyline格式存放，参考civil3d的做法，和绘制习惯

2  道路建模，使用rCs窗口，参考图3鸿业的建模方式 
1 需要明确横断面的作用范围（桩号范围，需要方便修改存储）2 明确标高位置 ，在那个板块中线的相对位置（选中板块后可调整），标高和设计纵断高差0,（将来和纵断面标高一致）
3 预设实际是一个窗口，可以存储各种已经做好的例子，以现在的预设为基础开发窗口，需要方便存在现在的方案，和切换方案。

4板块添加的时候可以按照图4 ，选中类型，然后添加，
右侧添加属性窗口，可以修改板块的属性，主要是宽度，厚度，路面结构，参考图6 是否有左右 立缘石（）
因为板块间有些时候是有高差的，需要立缘石。还有坡度，坡度向下为正，向上为负，可以参考图5，

**绘制道路的横断面图（建筑图）**
需要绘制到AutoCAD中，修改原来做法
1 出图比例遵循 hy面板 标注 单位 均按照 面板 设置
2 出图的使用用户可以选择单线，还是带结构厚度轮廓先出图（我的建议是先整体建好模型，单线出图可以只选择建好模型的上表面竖直方向投影）

有了道路横断面以后，就可以生成道路分段的平面模型（出道路平面图的时候只考虑上表面）然后考虑道路交叉口建模，绘图

在这个过程中最终要的一点是系统能够保障
在AutoCAD中绘制的图形没有特殊的类别，全部采用AutoCAD自由类型，AutoCAD的图形 需要能够和自己的模型随时转换
1 比如道路的横断面建模，车行道，面层 基层 垫层 中的任何一层的几何参数，可由AutoCAD选择的图形提供。要求必须是AutoCAD闭合图形Polyline就可以
最主要强调逻辑，所有能够从AutoCAD中提取的参数，均设置一个命令，从AutoCAD中提取。举例，绿化带我在AutoCAD中修改了对应于模型的绿化带集合信息，可以自动添加到模型（记录修改版本信息，类似git，用户可以迅速比对，可以采用rLa命令中窗口中的瞬态黄线处理），模型记录后可以随时方便生成。添加数据整理命令，例如去小数点，取整，放大缩小 数据功能
2 由道路横断面生成的模型，是可以有道路平面图，修正轮廓调整的，模型随之变化，记忆生成路径
3 十字路口各个条带交接处理，有的道路3条带，有的2条带，默认绿化带等级最低，优先连接其他（帮我设计表格处理，不同幅面路交叉处理的时候应该如何连接）需要设计一个wpf窗口和hyRoadCs命令窗口类似（帮我按功能优化）

需要符合LandXML
首先我需要完善我的rLa窗口
1 当我在PI参数面板调整的时候，调整完第一个点，切换第二个点，第一个点没有记录。

2 它应当是一个WPF，不是PaletteSet。
我需要一旦选取一条Polyline之后，就应当存储它的信息，rLa 添加命令，在拾取的旁边，绘制当前选中Polyline。需要能够方便的输入输出和AutoCAD有一个交互，无论选择什么图层的Polyline，绘制出去的时候都固定图层 用户拾取。
调整好的图形，绘制到AutoCAD的时候 不同区域要颜色区分 

我需要一旦选取一条Polyline a之后，就应当存储它的信息，绘出预览 命令 应当绘制绘制a到AutoCAD模型空间中。需要能够方便的输入输出和AutoCAD有一个交互，无论选择什么图层的Polyline，绘制出去的时候都固定图层 用户拾取。
调整好的图形，瞬态黄线应当变为 平面线位 存储到系统 ，绘制到AutoCAD的时候 不同区域要颜色区分 
文件系统要合理规划