# 001-从层级结构迁入-2026-04-25-200000

## 起因
按 Feature-Sliced 计划，将 OverKill 自 `Domain/Services` 与 `Presentation/Commands` 迁入 `Features/OverKill`，命名空间 `HyCADTool.Features.OverKill`。

## 改动
- `LineOverKillService.cs`、`OverKillCommand.cs` 移动并改命名空间
- `ReCall/commands.json`：`HYOV` / `HYOVSET` 的 `type` 指向新类型全名
- `AutofacModule` 注册 `OverKillCommand`

## 验证
- `dotnet build HyCADtoolGpt.sln`
- AutoCAD：C2 后执行 HYOV
