# 热加载测试机制说明

## 🎯 核心思想

**仿照原项目的 TestCommand.cs**，创建 `RefactoredTestCommand.cs`，实现：
- ✅ 只重新编译 `HyCADTool.Refactored.dll`
- ✅ 无需重启 AutoCAD
- ✅ 测试通过后注释，写新测试

---

## 📂 文件结构

```
hy-cad-tool/
├── ReCall/
│   ├── ReCall.dll              ← 一次性加载，不再修改
│   └── Recall - -HyCADTool - 复制.cs  ← 已更新，加载 RefactoredTestCommand
│
├── HyCADTool.Refactored/
│   └── Test/
│       ├── RefactoredTestCommand.cs  ← 【新】当前测试命令（仿照原 TestCommand.cs）
│       ├── Phase1TestCommand.cs       ← 已完成的测试（通过 C1P1 调用）
│       ├── Phase2TestCommand.cs       ← 已完成的测试（通过 C1P2 调用）
│       ├── Phase3TestCommand.cs       ← 已完成的测试（通过 C1P3 调用）
│       ├── Phase4TestCommand.cs       ← 已完成的测试（通过 C1P4 调用）
│       └── Phase5_0TestCommand.cs     ← 已完成的测试（通过 C1P50 调用）
```

---

## 🚀 使用流程

### 1. 首次加载（AutoCAD 启动后执行一次）

```
命令: NETLOAD
文件: ReCall/bin/Debug/ReCall.dll
```

### 2. 加载插件

```
命令: C2
输出: 插件加载成功
```

### 3. 运行测试

```
命令: C1
输出: 测试结果
```

### 4. 修改代码并重新测试（无需重启 AutoCAD）

```powershell
# 步骤 1: 编译
MSBuild.exe HyCADtoolGpt.sln /t:HyCADTool_Refactored /p:Configuration=Debug /v:minimal

# 步骤 2: 重新加载
AutoCAD> C2

# 步骤 3: 运行测试
AutoCAD> C1
```

---

## 📝 测试代码结构（RefactoredTestCommand.cs）

```csharp
[CommandMethod("C1", CommandFlags.Modal | CommandFlags.UsePickSet)]
public static void Test()
{
    var doc = AcApp.DocumentManager.MdiActiveDocument;
    var ed = doc.Editor;

    try
    {
        // ============================================================
        // 测试 1: ReinPanel 参数同步测试（当前测试）
        // ============================================================
        TestReinPanelParameterSync(ed);

        // ============================================================
        // 已完成的测试（已注释）
        // ============================================================
        // TestServiceLocatorInitialization(ed);
        // TestSelectionService(ed);
        // TestPanelManager(ed);
    }
    catch (System.Exception ex)
    {
        ed.WriteMessage($"\n❌ 测试失败: {ex.Message}\n");
    }
}
```

---

## 🔄 测试流程（仿照原 TestCommand.cs）

### 原项目做法（TestCommand.cs）

```csharp
[CommandMethod("te")]
public static void Test()
{
    // 当前测试（活跃）
    HyCommand.ReinOutside();
    
    // 已完成测试（已注释）
    //HyCommand.ShowPanel();
    //HyCommand.DrawElevationWithJig();
    //HyCommand.UpdateElevationText();
}
```

### 新项目做法（RefactoredTestCommand.cs）

```csharp
[CommandMethod("C1")]
public static void Test()
{
    // 当前测试（活跃）
    TestReinPanelParameterSync(ed);
    
    // 已完成测试（已注释）
    //TestServiceLocatorInitialization(ed);
    //TestSelectionService(ed);
}
```

---

## 🎯 优势对比

| 特性 | Phase1/2/3/4/5_0TestCommand | RefactoredTestCommand |
|------|---------------------------|----------------------|
| **命令名称** | `C1P1`, `C1P2`, `C1P3`, `C1P4`, `C1P50` | `C1` |
| **测试方式** | 综合测试（所有测试一起运行） | 迭代测试（当前测试 + 已注释测试） |
| **适用场景** | 阶段性验收 | 日常开发 |
| **修改频率** | 低（阶段完成后不再修改） | 高（每个新功能都需要修改） |
| **热加载** | ✅ 支持 | ✅ 支持 |

---

## 📊 命令对照表

| 命令 | 功能 | 文件 |
|------|------|------|
| `C2` | 重新加载插件 | `ReCall.dll` |
| `C1` | 当前测试（迭代开发） | `RefactoredTestCommand.cs` |
| `C1P1` | 阶段 1 综合测试 | `Phase1TestCommand.cs` |
| `C1P2` | 阶段 2 综合测试 | `Phase2TestCommand.cs` |
| `C1P3` | 阶段 3 综合测试 | `Phase3TestCommand.cs` |
| `C1P4` | 阶段 4 综合测试 | `Phase4TestCommand.cs` |
| `C1P50` | 阶段 5.0 综合测试 | `Phase5_0TestCommand.cs` |

---

## 🔧 ReCall.dll 的修改（已完成）

### 修改内容

```csharp
// ReCall/Recall - -HyCADTool - 复制.cs (Line 120-125)

// 【新】加载当前测试命令（RefactoredTestCommand.Test）
var typeRefactored = assembly.GetType("HyCADTool.Refactored.Test.RefactoredTestCommand") 
    ?? throw new InvalidOperationException("未找到类型 'RefactoredTestCommand'");
var methodRefactored = typeRefactored.GetMethod("Test") 
    ?? throw new InvalidOperationException("未找到方法 'Test'");
cmd1Action = () => methodRefactored.Invoke(null, null);
```

### 注意事项
- ⚠️ 此修改已完成，**无需重启 AutoCAD**
- ⚠️ 后续开发中 **只修改 `RefactoredTestCommand.cs`**
- ⚠️ **不要修改 `ReCall.dll` 源码**（否则需要重启 AutoCAD）

---

## 📖 示例：编写新测试

### 步骤 1: 注释当前测试

```csharp
[CommandMethod("C1")]
public static void Test()
{
    // 已完成的测试（已注释）
    //TestReinPanelParameterSync(ed);  // ← 测试通过后注释
}
```

### 步骤 2: 编写新测试

```csharp
[CommandMethod("C1")]
public static void Test()
{
    // 当前测试（活跃）
    TestFilterPanelFunctionality(ed);  // ← 新测试
    
    // 已完成的测试（已注释）
    //TestReinPanelParameterSync(ed);
}

private static void TestFilterPanelFunctionality(Editor ed)
{
    ed.WriteMessage("\n=== 测试 2: FilterPanel 功能测试 ===\n");
    // ... 测试代码 ...
}
```

### 步骤 3: 编译并测试

```
MSBuild → C2 → C1
```

---

## ✅ 当前状态

- [x] `RefactoredTestCommand.cs` 已创建
- [x] `ReCall.dll` 已更新（加载 `RefactoredTestCommand.Test`）
- [x] 编译成功（`HyCADTool.Refactored.dll`）
- [x] 测试指南已生成（`Plan/ReinPanel参数同步测试指南.md`）
- [ ] 等待用户在 AutoCAD 中测试

---

## 🎯 下一步

1. **在 AutoCAD 中执行**:
   ```
   C2   (重新加载插件)
   C1   (运行测试)
   ```

2. **查看测试结果**，确认：
   - ✅ 参数同步功能正常
   - ✅ 原有命令 (`GB1`, `GB2`) 能正常使用

3. **测试通过后**：
   - 注释 `TestReinPanelParameterSync(ed);`
   - 编写下一个测试（如 `TestFilterPanelFunctionality`）

---

**🚀 现在可以开始测试了！**






