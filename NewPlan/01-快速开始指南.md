# HyCADTool.Refactored - 快速开始指南

> **欢迎使用 HyCADTool.Refactored！**  
> Clean Architecture + DDD 架构的 AutoCAD 插件

---

## 🚀 5 分钟快速上手

### 1. 加载插件

```
AutoCAD 命令行：
NETLOAD

选择文件：
HyCADTool.Refactored\bin\Debug\HyCADTool.Refactored.dll
```

### 2. 运行测试

```
命令: HYTEST
```

**期望输出**:
```
╔════════════════════════════════════════════════════╗
║      HyCADTool.Refactored - 完整测试套件          ║
╚════════════════════════════════════════════════════╝

【阶段 1】配置层测试
▶ 全局配置... ✓
▶ 模块配置... ✓
▶ 配置更新... ✓

【阶段 2】服务层测试
▶ 绘制服务... ✓
▶ 编辑器服务... ✓
▶ 数据库服务... ✓

【阶段 3】Domain 算法层测试
▶ 点算法... ✓
▶ 线算法... ✓
▶ 多边形算法... ✓
▶ 角度计算... ✓
▶ 距离计算... ✓

══════════════════════════════════════════════════
测试完成: 11/11 通过 ✓
```

✅ 如果看到 **11/11 通过**，说明一切正常！

---

## 💡 核心使用示例

### 1. 使用服务

```csharp
using HyCADTool.Refactored.Infrastructure.DI;
using HyCADTool.Refactored.Domain.Interfaces;

[CommandMethod("MYDRAWCMD")]
public void MyDrawCommand()
{
    var drawingService = ServiceLocator.Resolve<IDrawingService>();
    var editorService = ServiceLocator.Resolve<IEditorService>();
    
    var pt1 = editorService.GetPoint("选择起点");
    if (!pt1.HasValue) return;
    
    var pt2 = editorService.GetPoint("选择终点", pt1.Value);
    if (!pt2.HasValue) return;
    
    var lineId = drawingService.DrawLine(pt1.Value, pt2.Value);
    editorService.WriteMessage($"\n已创建直线: {lineId}");
}
```

### 2. 使用 Domain 算法

```csharp
using HyCADTool.Refactored.Domain.ValueObjects.Geometry;
using HyCADTool.Refactored.Domain.Services.GeometryAlgorithms;

var p1 = new Point2D(0, 0);
var p2 = new Point2D(10, 10);

double distance = PointAlgorithms.Distance(p1, p2);
var midpoint = PointAlgorithms.Midpoint(p1, p2);

// 这些算法 100% 可迁移到 Blender Python！
```

---

## 📚 深入学习

| 文档 | 内容 |
|------|------|
| [00-项目总览与导航](./00-项目总览与导航.md) | **从这里开始！** |
| [02-下一步行动指南](./02-下一步行动指南.md) | 继续开发 |
| [10-详细计划](./10-详细计划.md) | 完整重构计划 |

---

**最后更新**: 2025-10-13

