# 阶段 2: 服务层重构详细设计

> **开始时间**: 2025-10-13  
> **预计工期**: 3-4 天  
> **目标**: 建立完整的 CAD 基础服务层，为后续工具和命令提供统一的操作接口

---

## 📋 目录

1. [当前状态分析](#当前状态分析)
2. [目标与范围](#目标与范围)
3. [架构设计](#架构设计)
4. [详细任务](#详细任务)
5. [验收标准](#验收标准)

---

## 当前状态分析

### 已完成服务（6 个）

| 服务 | 文件 | 状态 | 功能覆盖 |
|------|------|------|----------|
| **LayerService** | `LayerService.cs` | ✅ 完善 | 创建、删除、设置图层、检查存在性 |
| **StyleService** | `StyleService.cs` | ✅ 完善 | 文本样式、标注样式、多重引线样式 |
| **SelectionService** | `SelectionService.cs` | ✅ 完善 | 实体选择、过滤、高亮显示 |
| **AutoCadGeometryService** | `AutoCadGeometryService.cs` | ✅ 完善 | Clipper2 布尔运算、凸包、交点 |
| **GeometryConverter** | `GeometryConverter.cs` | ✅ 可用 | AutoCAD ↔ Domain 几何对象转换 |
| **LineRepository** | `LineRepository.cs` | ✅ 可用 | 线段数据访问 |

### 原项目服务分析

| 原服务 | 文件 | 主要功能 | 迁移策略 |
|--------|------|----------|----------|
| **AutoCadService** | `AutoCadService - 复制.cs` | 图层、绘制、选择、表格 | 拆分到新服务 |
| **ConfigService** | `ConfigService.cs` | 配置管理 | ✅ 已重构（阶段1） |
| **AreaFactory** | `AreaFactory.cs` | 桩基区域工厂 | 暂不迁移（阶段4） |

### 功能缺口分析

根据原 `AutoCadService` 的功能和 ZTools 的依赖，当前缺少：

| 功能类别 | 缺失服务 | 优先级 | 说明 |
|----------|----------|--------|------|
| **实体绘制** | DrawingService | **P0** | 绘制线、圆、多段线、文本、表格等 |
| **事务管理** | TransactionHelper | **P0** | 简化事务操作，提供扩展方法 |
| **编辑器交互** | EditorService | **P1** | 用户输入、提示、选点等 |
| **数据库操作** | DatabaseService | **P1** | 数据库级通用操作 |
| **Block操作** | BlockService | **P2** | Block 表、BlockReference 管理 |
| **实体修改** | EntityService | **P2** | 实体通用操作（移动、旋转、镜像等） |

---

## 目标与范围

### 主要目标

1. ✅ **补全核心服务**: 创建 DrawingService, TransactionHelper, EditorService, DatabaseService
2. ✅ **统一服务接口**: 定义清晰的接口契约
3. ✅ **DI 集成**: 注册到 Autofac 容器
4. ✅ **测试验证**: 创建测试命令验证服务功能
5. ✅ **文档完善**: 编写服务使用文档

### 范围界定

**包含**:
- ✅ P0 和 P1 优先级的服务
- ✅ 常用 CAD 操作封装
- ✅ 事务管理辅助工具
- ✅ 编辑器交互封装

**不包含**:
- ❌ Block 服务（P2，阶段3处理）
- ❌ 实体修改服务（P2，阶段3处理）
- ❌ 复杂的业务逻辑（阶段5处理）

---

## 架构设计

### 服务层架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      Presentation Layer                         │
│                       (Commands / Panels)                        │
└────────────────────────────┬────────────────────────────────────┘
                             │ 依赖
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Infrastructure.AutoCAD.Services               │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  基础服务 (Infrastructure Services)                       │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │  • LayerService        - 图层管理                         │  │
│  │  • StyleService        - 样式管理                         │  │
│  │  • SelectionService    - 实体选择                         │  │
│  │  • DrawingService      - 实体绘制 (NEW)                   │  │
│  │  • EditorService       - 编辑器交互 (NEW)                 │  │
│  │  • DatabaseService     - 数据库操作 (NEW)                 │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  辅助工具 (Utilities)                                     │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │  • TransactionHelper   - 事务扩展方法 (NEW)               │  │
│  │  • EntityHelper        - 实体扩展方法 (NEW)               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  领域服务 (Domain Services)                               │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │  • AutoCadGeometryService - 几何运算                      │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────┬────────────────────────────────────┘
                              │ 实现
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                       Domain.Interfaces                          │
│                                                                  │
│  • ILayerService           • IDrawingService    (NEW)            │
│  • IStyleService           • IEditorService     (NEW)            │
│  • ISelectionService       • IDatabaseService   (NEW)            │
│  • IGeometryService                                              │
└─────────────────────────────────────────────────────────────────┘
```

### 服务职责划分

| 服务 | 职责 | 典型方法 |
|------|------|----------|
| **DrawingService** | 在 ModelSpace 或指定 Block 中绘制实体 | `DrawLine()`, `DrawPolyline()`, `DrawCircle()`, `DrawText()` |
| **EditorService** | 封装用户交互（Editor 操作） | `GetPoint()`, `GetDistance()`, `GetKeyword()`, `WriteMessage()` |
| **DatabaseService** | 数据库级通用操作 | `GetAllEntities()`, `GetBlockTable()`, `GetLayerTable()` |
| **TransactionHelper** | 事务管理辅助（扩展方法） | `GetEntity<T>()`, `ModifyEntity<T>()`, `AddToModelSpace()` |
| **EntityHelper** | 实体通用操作（扩展方法） | `Clone()`, `Transform()`, `GetBounds()` |

---

## 详细任务

### 子阶段 2.1: 创建接口定义（0.5 天）

#### 任务清单

- [ ] 创建 `IDrawingService.cs`
- [ ] 创建 `IEditorService.cs`
- [ ] 创建 `IDatabaseService.cs`
- [ ] 审查已有接口（ILayerService, IStyleService, ISelectionService）

#### 文件结构

```
Domain/Interfaces/
├── ILayerService.cs           (已有)
├── IStyleService.cs           (已有)
├── ISelectionService.cs       (已有)
├── IGeometryService.cs        (已有)
├── IDrawingService.cs         (NEW)
├── IEditorService.cs          (NEW)
└── IDatabaseService.cs        (NEW)
```

#### 接口设计

##### IDrawingService.cs

```csharp
using Autodesk.AutoCAD.DatabaseServices;
using Autodesk.AutoCAD.Geometry;
using System.Collections.Generic;

namespace HyCADTool.Refactored.Domain.Interfaces
{
    /// <summary>
    /// 实体绘制服务接口
    /// </summary>
    public interface IDrawingService
    {
        // 基础绘制
        ObjectId DrawLine(Point3d start, Point3d end, string layerName = null);
        ObjectId DrawPolyline(IEnumerable<Point2d> points, string layerName = null, bool isClosed = false);
        ObjectId DrawCircle(Point3d center, double radius, string layerName = null);
        ObjectId DrawArc(Point3d center, double radius, double startAngle, double endAngle, string layerName = null);
        ObjectId DrawEllipse(Point3d center, Vector3d majorAxis, double radiusRatio, string layerName = null);
        
        // 文本绘制
        ObjectId DrawText(Point3d position, string content, double height, string layerName = null);
        ObjectId DrawMText(Point3d position, string content, double height, double width, string layerName = null);
        
        // 标注绘制
        ObjectId DrawAlignedDimension(Point3d start, Point3d end, Point3d textPoint, string layerName = null);
        ObjectId DrawRotatedDimension(Point3d start, Point3d end, Point3d textPoint, double rotation, string layerName = null);
        
        // 批量绘制
        ObjectId[] DrawEntities(IEnumerable<Entity> entities, string layerName = null);
        
        // 到指定 Block 绘制
        ObjectId DrawToBlock(Entity entity, string blockName, string layerName = null);
        
        // 删除实体
        void DeleteEntity(ObjectId entityId);
        void DeleteEntities(IEnumerable<ObjectId> entityIds);
    }
}
```

##### IEditorService.cs

```csharp
using Autodesk.AutoCAD.EditorInput;
using Autodesk.AutoCAD.Geometry;

namespace HyCADTool.Refactored.Domain.Interfaces
{
    /// <summary>
    /// 编辑器交互服务接口
    /// </summary>
    public interface IEditorService
    {
        // 消息输出
        void WriteMessage(string message);
        void WriteWarning(string message);
        void WriteError(string message);
        
        // 点输入
        Point3d? GetPoint(string prompt);
        Point3d? GetPoint(string prompt, Point3d basePoint);
        
        // 数值输入
        double? GetDistance(string prompt);
        double? GetAngle(string prompt);
        int? GetInteger(string prompt);
        
        // 字符串输入
        string GetString(string prompt, bool allowSpaces = true);
        
        // 关键字选择
        string GetKeyword(string prompt, params string[] keywords);
        
        // 确认
        bool GetYesNo(string prompt, bool defaultValue = true);
        
        // 实体选择（委托给 SelectionService）
        // 这里不重复定义，使用 ISelectionService
    }
}
```

##### IDatabaseService.cs

```csharp
using Autodesk.AutoCAD.DatabaseServices;
using System;
using System.Collections.Generic;

namespace HyCADTool.Refactored.Domain.Interfaces
{
    /// <summary>
    /// 数据库操作服务接口
    /// </summary>
    public interface IDatabaseService
    {
        // 数据库访问
        Database GetCurrentDatabase();
        
        // 表访问
        BlockTable GetBlockTable();
        LayerTable GetLayerTable();
        TextStyleTable GetTextStyleTable();
        DimStyleTable GetDimStyleTable();
        
        // 实体查询
        ObjectId[] GetAllEntitiesInModelSpace();
        ObjectId[] GetAllEntitiesInBlock(string blockName);
        ObjectId[] GetAllEntitiesByType<T>() where T : Entity;
        
        // 实体操作
        T GetEntity<T>(ObjectId entityId) where T : Entity;
        void ModifyEntity<T>(ObjectId entityId, Action<T> action) where T : Entity;
        
        // Block 操作
        bool BlockExists(string blockName);
        ObjectId GetBlockId(string blockName);
        ObjectId[] GetAllBlockReferences(string blockName);
    }
}
```

---

### 子阶段 2.2: 实现 DrawingService（1 天）

#### 文件位置

```
Infrastructure/AutoCAD/Services/DrawingService.cs
```

#### 实现要点

```csharp
using Autodesk.AutoCAD.DatabaseServices;
using Autodesk.AutoCAD.Geometry;
using HyCADTool.Refactored.Domain.Interfaces;
using System;
using System.Collections.Generic;
using System.Linq;
using AcApp = Autodesk.AutoCAD.ApplicationServices.Application;

namespace HyCADTool.Refactored.Infrastructure.AutoCAD.Services
{
    /// <summary>
    /// 实体绘制服务实现
    /// </summary>
    public class DrawingService : IDrawingService
    {
        private readonly ILayerService _layerService;

        public DrawingService(ILayerService layerService)
        {
            _layerService = layerService;
        }

        #region 基础绘制

        public ObjectId DrawLine(Point3d start, Point3d end, string layerName = null)
        {
            var doc = AcApp.DocumentManager.MdiActiveDocument;
            var db = doc.Database;

            using (doc.LockDocument())
            using (var tr = db.TransactionManager.StartTransaction())
            {
                try
                {
                    var bt = (BlockTable)tr.GetObject(db.BlockTableId, OpenMode.ForRead);
                    var btr = (BlockTableRecord)tr.GetObject(
                        bt[BlockTableRecord.ModelSpace], 
                        OpenMode.ForWrite
                    );

                    var line = new Line(start, end);
                    
                    if (!string.IsNullOrEmpty(layerName))
                    {
                        line.Layer = layerName;
                    }

                    var id = btr.AppendEntity(line);
                    tr.AddNewlyCreatedDBObject(line, true);
                    tr.Commit();

                    return id;
                }
                catch
                {
                    tr.Abort();
                    throw;
                }
            }
        }

        public ObjectId DrawPolyline(IEnumerable<Point2d> points, string layerName = null, bool isClosed = false)
        {
            var doc = AcApp.DocumentManager.MdiActiveDocument;
            var db = doc.Database;

            using (doc.LockDocument())
            using (var tr = db.TransactionManager.StartTransaction())
            {
                try
                {
                    var bt = (BlockTable)tr.GetObject(db.BlockTableId, OpenMode.ForRead);
                    var btr = (BlockTableRecord)tr.GetObject(
                        bt[BlockTableRecord.ModelSpace], 
                        OpenMode.ForWrite
                    );

                    var polyline = new Polyline();
                    int index = 0;
                    foreach (var point in points)
                    {
                        polyline.AddVertexAt(index++, point, 0, 0, 0);
                    }
                    polyline.Closed = isClosed;

                    if (!string.IsNullOrEmpty(layerName))
                    {
                        polyline.Layer = layerName;
                    }

                    var id = btr.AppendEntity(polyline);
                    tr.AddNewlyCreatedDBObject(polyline, true);
                    tr.Commit();

                    return id;
                }
                catch
                {
                    tr.Abort();
                    throw;
                }
            }
        }

        public ObjectId DrawCircle(Point3d center, double radius, string layerName = null)
        {
            var doc = AcApp.DocumentManager.MdiActiveDocument;
            var db = doc.Database;

            using (doc.LockDocument())
            using (var tr = db.TransactionManager.StartTransaction())
            {
                try
                {
                    var bt = (BlockTable)tr.GetObject(db.BlockTableId, OpenMode.ForRead);
                    var btr = (BlockTableRecord)tr.GetObject(
                        bt[BlockTableRecord.ModelSpace], 
                        OpenMode.ForWrite
                    );

                    var circle = new Circle(center, Vector3d.ZAxis, radius);

                    if (!string.IsNullOrEmpty(layerName))
                    {
                        circle.Layer = layerName;
                    }

                    var id = btr.AppendEntity(circle);
                    tr.AddNewlyCreatedDBObject(circle, true);
                    tr.Commit();

                    return id;
                }
                catch
                {
                    tr.Abort();
                    throw;
                }
            }
        }

        // ... 其他绘制方法类似实现

        #endregion

        #region 批量绘制

        public ObjectId[] DrawEntities(IEnumerable<Entity> entities, string layerName = null)
        {
            var doc = AcApp.DocumentManager.MdiActiveDocument;
            var db = doc.Database;
            var ids = new List<ObjectId>();

            using (doc.LockDocument())
            using (var tr = db.TransactionManager.StartTransaction())
            {
                try
                {
                    var bt = (BlockTable)tr.GetObject(db.BlockTableId, OpenMode.ForRead);
                    var btr = (BlockTableRecord)tr.GetObject(
                        bt[BlockTableRecord.ModelSpace], 
                        OpenMode.ForWrite
                    );

                    foreach (var entity in entities)
                    {
                        if (!string.IsNullOrEmpty(layerName))
                        {
                            entity.Layer = layerName;
                        }

                        var id = btr.AppendEntity(entity);
                        tr.AddNewlyCreatedDBObject(entity, true);
                        ids.Add(id);
                    }

                    tr.Commit();
                    return ids.ToArray();
                }
                catch
                {
                    tr.Abort();
                    throw;
                }
            }
        }

        #endregion

        #region 删除实体

        public void DeleteEntity(ObjectId entityId)
        {
            var doc = AcApp.DocumentManager.MdiActiveDocument;
            var db = doc.Database;

            using (doc.LockDocument())
            using (var tr = db.TransactionManager.StartTransaction())
            {
                try
                {
                    var entity = tr.GetObject(entityId, OpenMode.ForWrite) as Entity;
                    if (entity != null && !entity.IsErased)
                    {
                        entity.Erase();
                    }
                    tr.Commit();
                }
                catch
                {
                    tr.Abort();
                    throw;
                }
            }
        }

        public void DeleteEntities(IEnumerable<ObjectId> entityIds)
        {
            var doc = AcApp.DocumentManager.MdiActiveDocument;
            var db = doc.Database;

            using (doc.LockDocument())
            using (var tr = db.TransactionManager.StartTransaction())
            {
                try
                {
                    foreach (var id in entityIds)
                    {
                        var entity = tr.GetObject(id, OpenMode.ForWrite) as Entity;
                        if (entity != null && !entity.IsErased)
                        {
                            entity.Erase();
                        }
                    }
                    tr.Commit();
                }
                catch
                {
                    tr.Abort();
                    throw;
                }
            }
        }

        #endregion
    }
}
```

---

### 子阶段 2.3: 实现 EditorService（0.5 天）

#### 文件位置

```
Infrastructure/AutoCAD/Services/EditorService.cs
```

#### 实现示例（部分）

```csharp
using Autodesk.AutoCAD.EditorInput;
using Autodesk.AutoCAD.Geometry;
using HyCADTool.Refactored.Domain.Interfaces;
using AcApp = Autodesk.AutoCAD.ApplicationServices.Application;

namespace HyCADTool.Refactored.Infrastructure.AutoCAD.Services
{
    /// <summary>
    /// 编辑器交互服务实现
    /// </summary>
    public class EditorService : IEditorService
    {
        public void WriteMessage(string message)
        {
            var doc = AcApp.DocumentManager.MdiActiveDocument;
            doc?.Editor.WriteMessage($"\n{message}");
        }

        public void WriteWarning(string message)
        {
            var doc = AcApp.DocumentManager.MdiActiveDocument;
            doc?.Editor.WriteMessage($"\n⚠️ {message}");
        }

        public void WriteError(string message)
        {
            var doc = AcApp.DocumentManager.MdiActiveDocument;
            doc?.Editor.WriteMessage($"\n✗ {message}");
        }

        public Point3d? GetPoint(string prompt)
        {
            var doc = AcApp.DocumentManager.MdiActiveDocument;
            var ed = doc.Editor;

            var options = new PromptPointOptions($"\n{prompt}: ");
            var result = ed.GetPoint(options);

            if (result.Status == PromptStatus.OK)
            {
                return result.Value;
            }

            return null;
        }

        public double? GetDistance(string prompt)
        {
            var doc = AcApp.DocumentManager.MdiActiveDocument;
            var ed = doc.Editor;

            var options = new PromptDistanceOptions($"\n{prompt}: ")
            {
                AllowNone = false,
                AllowZero = false
            };

            var result = ed.GetDistance(options);

            if (result.Status == PromptStatus.OK)
            {
                return result.Value;
            }

            return null;
        }

        public string GetKeyword(string prompt, params string[] keywords)
        {
            var doc = AcApp.DocumentManager.MdiActiveDocument;
            var ed = doc.Editor;

            var options = new PromptKeywordOptions($"\n{prompt}: ");
            foreach (var keyword in keywords)
            {
                options.Keywords.Add(keyword);
            }

            var result = ed.GetKeywords(options);

            if (result.Status == PromptStatus.OK)
            {
                return result.StringResult;
            }

            return null;
        }

        // ... 其他方法
    }
}
```

---

### 子阶段 2.4: 实现 DatabaseService（0.5 天）

#### 文件位置

```
Infrastructure/AutoCAD/Services/DatabaseService.cs
```

---

### 子阶段 2.5: 创建 TransactionHelper（0.5 天）

#### 文件位置

```
Infrastructure/AutoCAD/Utilities/TransactionHelper.cs
```

#### 实现示例

```csharp
using Autodesk.AutoCAD.DatabaseServices;
using System;

namespace HyCADTool.Refactored.Infrastructure.AutoCAD.Utilities
{
    /// <summary>
    /// 事务管理扩展方法
    /// </summary>
    public static class TransactionHelper
    {
        /// <summary>
        /// 从事务中获取实体（只读）
        /// </summary>
        public static T GetEntity<T>(this Transaction trans, ObjectId id) where T : Entity
        {
            return trans.GetObject(id, OpenMode.ForRead) as T;
        }

        /// <summary>
        /// 从事务中获取实体（可写）
        /// </summary>
        public static T GetEntityForWrite<T>(this Transaction trans, ObjectId id) where T : Entity
        {
            return trans.GetObject(id, OpenMode.ForWrite) as T;
        }

        /// <summary>
        /// 修改实体（自动升级写权限）
        /// </summary>
        public static void ModifyEntity<T>(this Transaction trans, ObjectId id, Action<T> action) 
            where T : Entity
        {
            var entity = trans.GetObject(id, OpenMode.ForWrite) as T;
            if (entity != null)
            {
                action(entity);
            }
        }

        /// <summary>
        /// 添加实体到 ModelSpace
        /// </summary>
        public static ObjectId AddToModelSpace(this Transaction trans, Database db, Entity entity)
        {
            var bt = (BlockTable)trans.GetObject(db.BlockTableId, OpenMode.ForRead);
            var btr = (BlockTableRecord)trans.GetObject(
                bt[BlockTableRecord.ModelSpace], 
                OpenMode.ForWrite
            );

            var id = btr.AppendEntity(entity);
            trans.AddNewlyCreatedDBObject(entity, true);
            return id;
        }

        /// <summary>
        /// 批量添加实体到 ModelSpace
        /// </summary>
        public static ObjectId[] AddEntitiesToModelSpace(
            this Transaction trans, 
            Database db, 
            params Entity[] entities)
        {
            var bt = (BlockTable)trans.GetObject(db.BlockTableId, OpenMode.ForRead);
            var btr = (BlockTableRecord)trans.GetObject(
                bt[BlockTableRecord.ModelSpace], 
                OpenMode.ForWrite
            );

            var ids = new ObjectId[entities.Length];
            for (int i = 0; i < entities.Length; i++)
            {
                ids[i] = btr.AppendEntity(entities[i]);
                trans.AddNewlyCreatedDBObject(entities[i], true);
            }
            return ids;
        }
    }
}
```

---

### 子阶段 2.6: 注册服务到 Autofac（0.5 天）

#### 更新 AutofacModule.cs

```csharp
// ===== 阶段 2: 服务层 =====

// 绘制服务（单例）
builder.RegisterType<DrawingService>()
    .As<IDrawingService>()
    .SingleInstance();

// 编辑器服务（单例）
builder.RegisterType<EditorService>()
    .As<IEditorService>()
    .SingleInstance();

// 数据库服务（单例）
builder.RegisterType<DatabaseService>()
    .As<IDatabaseService>()
    .SingleInstance();
```

---

### 子阶段 2.7: 创建测试命令（0.5 天）

#### 文件位置

```
Presentation/Commands/TestServiceCommand.cs
```

#### 测试命令设计

```csharp
using Autodesk.AutoCAD.Runtime;
using Autodesk.AutoCAD.Geometry;
using HyCADTool.Refactored.Infrastructure.Configuration;
using HyCADTool.Refactored.Domain.Interfaces;
using System.Linq;

namespace HyCADTool.Refactored.Presentation.Commands
{
    public class TestServiceCommand
    {
        /// <summary>
        /// 测试 DrawingService - 绘制基础图形
        /// </summary>
        [CommandMethod("HYTESTDRAW")]
        public void TestDrawing()
        {
            var drawingService = ServiceLocator.Resolve<IDrawingService>();
            var editorService = ServiceLocator.Resolve<IEditorService>();

            try
            {
                editorService.WriteMessage("开始测试 DrawingService...");

                // 测试 1: 绘制线段
                var lineId = drawingService.DrawLine(
                    new Point3d(0, 0, 0),
                    new Point3d(100, 100, 0)
                );
                editorService.WriteMessage($"✓ 线段已绘制: {lineId}");

                // 测试 2: 绘制多段线
                var points = new[]
                {
                    new Point2d(0, 0),
                    new Point2d(100, 0),
                    new Point2d(100, 100),
                    new Point2d(0, 100)
                };
                var polyId = drawingService.DrawPolyline(points, isClosed: true);
                editorService.WriteMessage($"✓ 多段线已绘制: {polyId}");

                // 测试 3: 绘制圆
                var circleId = drawingService.DrawCircle(
                    new Point3d(50, 50, 0),
                    30
                );
                editorService.WriteMessage($"✓ 圆已绘制: {circleId}");

                editorService.WriteMessage("✓ DrawingService 测试通过！");
            }
            catch (System.Exception ex)
            {
                editorService.WriteError($"测试失败: {ex.Message}");
            }
        }

        /// <summary>
        /// 测试 EditorService - 用户交互
        /// </summary>
        [CommandMethod("HYTESTEDITOR")]
        public void TestEditor()
        {
            var editorService = ServiceLocator.Resolve<IEditorService>();

            try
            {
                editorService.WriteMessage("开始测试 EditorService...");

                // 测试 1: 获取点
                var point = editorService.GetPoint("请选择一个点");
                if (point.HasValue)
                {
                    editorService.WriteMessage($"✓ 已选择点: {point.Value}");
                }

                // 测试 2: 获取距离
                var distance = editorService.GetDistance("请输入距离");
                if (distance.HasValue)
                {
                    editorService.WriteMessage($"✓ 已输入距离: {distance.Value}");
                }

                // 测试 3: 关键字选择
                var keyword = editorService.GetKeyword(
                    "请选择选项", 
                    "选项1", "选项2", "选项3"
                );
                if (keyword != null)
                {
                    editorService.WriteMessage($"✓ 已选择: {keyword}");
                }

                editorService.WriteMessage("✓ EditorService 测试通过！");
            }
            catch (System.Exception ex)
            {
                editorService.WriteError($"测试失败: {ex.Message}");
            }
        }

        /// <summary>
        /// 测试 DatabaseService - 数据库查询
        /// </summary>
        [CommandMethod("HYTESTDB")]
        public void TestDatabase()
        {
            var databaseService = ServiceLocator.Resolve<IDatabaseService>();
            var editorService = ServiceLocator.Resolve<IEditorService>();

            try
            {
                editorService.WriteMessage("开始测试 DatabaseService...");

                // 测试 1: 获取所有实体
                var allEntities = databaseService.GetAllEntitiesInModelSpace();
                editorService.WriteMessage($"✓ ModelSpace 中共有 {allEntities.Length} 个实体");

                // 测试 2: 获取特定类型实体
                var lines = databaseService.GetAllEntitiesByType<Autodesk.AutoCAD.DatabaseServices.Line>();
                editorService.WriteMessage($"✓ 共有 {lines.Length} 条线段");

                // 测试 3: 获取所有图层
                var layerTable = databaseService.GetLayerTable();
                int layerCount = 0;
                foreach (Autodesk.AutoCAD.DatabaseServices.ObjectId id in layerTable)
                {
                    layerCount++;
                }
                editorService.WriteMessage($"✓ 共有 {layerCount} 个图层");

                editorService.WriteMessage("✓ DatabaseService 测试通过！");
            }
            catch (System.Exception ex)
            {
                editorService.WriteError($"测试失败: {ex.Message}");
            }
        }
    }
}
```

---

### 子阶段 2.8: 更新 TestRunner（0.5 天）

在 `Test/TestRunner.cs` 中添加阶段2的测试：

```csharp
public static class TestRunner
{
    public static void RunAllTests()
    {
        WriteHeader("HyCADTool.Refactored - 完整测试套件");

        // 阶段 1: 配置层测试
        WriteSection("【阶段 1】配置层测试");
        TestGlobalConfiguration();
        TestModuleConfiguration();
        TestConfigurationUpdate();

        // 阶段 2: 服务层测试 (NEW)
        WriteSection("【阶段 2】服务层测试");
        TestDrawingService();
        TestEditorService();
        TestDatabaseService();

        WriteSummary(passedTests, totalTests);
    }

    private static void TestDrawingService()
    {
        try
        {
            Write("▶ 绘制服务... ");
            
            var drawingService = ServiceLocator.Resolve<IDrawingService>();
            
            // 测试绘制
            var lineId = drawingService.DrawLine(
                new Point3d(0, 0, 0),
                new Point3d(100, 0, 0)
            );
            
            if (lineId != ObjectId.Null)
            {
                WriteSuccess();
                passedTests++;
            }
            else
            {
                WriteFail();
            }
            
            totalTests++;
        }
        catch (Exception ex)
        {
            WriteFail($"{ex.Message}");
            totalTests++;
        }
    }

    // ... 其他测试方法类似
}
```

---

## 验收标准

### 功能验收

| 检查项 | 标准 | 验证方式 |
|--------|------|----------|
| **服务接口** | 3 个新接口定义完整 | 代码审查 |
| **服务实现** | DrawingService, EditorService, DatabaseService 实现完整 | 代码审查 + 测试 |
| **辅助工具** | TransactionHelper 提供常用扩展方法 | 代码审查 |
| **DI 注册** | 所有服务已注册到 Autofac | AutofacModule.cs 审查 |
| **测试通过** | 6/6 测试通过（3 阶段1 + 3 阶段2） | 运行 HYTESTALL |

### 质量验收

| 检查项 | 标准 | 实际 |
|--------|------|------|
| **编译通过** | 0 错误、0 警告 | ✅ |
| **代码规范** | 符合 C# 编码规范 | ✅ |
| **注释完整** | 所有公共方法有 XML 注释 | ✅ |
| **异常处理** | 关键方法有异常保护 | ✅ |

### 测试命令清单

| 命令 | 功能 | 预期结果 |
|------|------|----------|
| `HYTESTDRAW` | 测试 DrawingService | 绘制线、多段线、圆成功 |
| `HYTESTEDITOR` | 测试 EditorService | 用户交互正常 |
| `HYTESTDB` | 测试 DatabaseService | 查询数据库成功 |
| `C1` | 运行所有测试 | 6/6 通过 |

---

## 时间估算

| 子阶段 | 任务 | 预计时间 | 实际时间 |
|--------|------|----------|----------|
| 2.1 | 创建接口定义 | 0.5 天 | |
| 2.2 | 实现 DrawingService | 1.0 天 | |
| 2.3 | 实现 EditorService | 0.5 天 | |
| 2.4 | 实现 DatabaseService | 0.5 天 | |
| 2.5 | 创建 TransactionHelper | 0.5 天 | |
| 2.6 | 注册服务到 Autofac | 0.5 天 | |
| 2.7 | 创建测试命令 | 0.5 天 | |
| 2.8 | 更新 TestRunner | 0.5 天 | |
| **总计** | | **4.5 天** | |

---

## 注意事项

### 避免过度工程化

- ✅ 服务方法应简洁、单一职责
- ✅ 不要为每个 AutoCAD API 都创建包装
- ✅ 只封装常用操作

### 保持一致性

- ✅ 所有服务返回值处理一致
- ✅ 异常处理策略统一
- ✅ 命名规范一致

### 性能考虑

- ✅ 服务注册为单例（无状态）
- ✅ 避免在服务中缓存可变状态
- ✅ 事务管理高效

---

## 下一步计划

完成阶段2后，进入**阶段3：通用工具层重构 (ZTools)**。

---

**文档创建时间**: 2025-10-13  
**预计开始时间**: 阶段1完成后  
**预计完成时间**: 阶段1完成后 4-5 天

