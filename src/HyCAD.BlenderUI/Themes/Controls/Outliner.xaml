<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
      自包含 Merge（pitfall B5）。
    -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/HyCAD.BlenderUI;component/Themes/Colors.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/HyCAD.BlenderUI;component/Themes/Metrics.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/HyCAD.BlenderUI;component/Themes/Fonts.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/HyCAD.BlenderUI;component/Themes/Controls/WidgetBackdrop.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/HyCAD.BlenderUI;component/Themes/Controls/ScrollBar.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!--
      OutlinerExpandToggle：TreeViewItem Header 行左侧的「展开三角」ToggleButton 命名样式
      （TargetType=ToggleButton 是原生类型，按 B1/B2 必须 x:Key）。
        Checked 时三角向下旋转 90°。
    -->
    <Style TargetType="{x:Type ToggleButton}" x:Key="OutlinerExpandToggle">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Width"  Value="14"/>
        <Setter Property="Height" Value="14"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment"   Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border x:Name="ArrowHost"
                            Background="Transparent"
                            Width="14" Height="14"
                            RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform>
                            <RotateTransform Angle="0"/>
                        </Border.RenderTransform>
                        <Path x:Name="Arrow"
                              Width="8" Height="8"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Data="M 0,0 L 4,4 L 0,8 Z"
                              Fill="{DynamicResource Brush_TextSecondary}"
                              Stretch="Uniform"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="ArrowHost" Property="RenderTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="90"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Arrow" Property="Fill" Value="{DynamicResource Brush_TextPrimary}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
      BlenderOutlinerItem 命名样式（TargetType=TreeViewItem 是原生类型，按 B1/B2 必须 x:Key）：
        24px 行：[Indent] + [Toggle 14] + [Header]。
        hover = Brush_ItemHover；selected = Brush_ItemSelected。
        TreeViewItem 本身只设 Header，Items 由 ItemsSource 自动展开。
    -->
    <Style TargetType="{x:Type TreeViewItem}" x:Key="BlenderOutlinerItem">
        <Setter Property="Background"     Value="Transparent"/>
        <Setter Property="Foreground"     Value="{DynamicResource Brush_TextPrimary}"/>
        <Setter Property="FontFamily"     Value="{DynamicResource Font_Main}"/>
        <Setter Property="FontSize"       Value="{DynamicResource Metric_FontMain}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment"   Value="Center"/>
        <Setter Property="Padding" Value="2,0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TreeViewItem}">
                    <StackPanel>
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                Height="22"
                                BorderThickness="0"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1"/>
                                    <ColumnDefinition Width="15"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0"
                                        Background="{DynamicResource Brush_BorderMid}"
                                        Opacity="0.45"
                                        Width="1"
                                        HorizontalAlignment="Left"
                                        Margin="0,2,0,2"/>
                                <ToggleButton x:Name="Expander"
                                              Grid.Column="1"
                                              ClickMode="Press"
                                              IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                              Style="{StaticResource OutlinerExpandToggle}"/>
                                <ContentPresenter x:Name="PART_Header"
                                                  Grid.Column="2"
                                                  ContentSource="Header"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"/>
                            </Grid>
                        </Border>
                        <ItemsPresenter x:Name="ItemsHost" Margin="14,0,0,0"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="False">
                            <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Brush_ItemHover}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Brush_ItemSelected}"/>
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Brush_AccentBlue}"/>
                            <Setter TargetName="Bd" Property="BorderThickness" Value="2,0,0,0"/>
                            <Setter Property="Foreground" Value="{DynamicResource Brush_ItemSelectedText}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
      BlenderOutliner 命名样式（TargetType=TreeView 是原生类型，按 B1/B2 必须 x:Key）：
        无边框 + 区域底色 + 默认 BlenderOutlinerItem ItemContainerStyle。
        用法：<TreeView Style="{StaticResource BlenderOutliner}">…
    -->
    <Style TargetType="{x:Type TreeView}" x:Key="BlenderOutliner">
        <Setter Property="Background"      Value="{DynamicResource Brush_RegionBack}"/>
        <Setter Property="Foreground"      Value="{DynamicResource Brush_TextPrimary}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource BlenderOutlinerItem}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TreeView}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <ScrollViewer Style="{StaticResource BlenderScrollViewer}"
                                      Focusable="False"
                                      HorizontalScrollBarVisibility="Auto"
                                      VerticalScrollBarVisibility="Auto"
                                      Padding="2">
                            <ItemsPresenter/>
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
