# 001-从层级结构迁入-2026-04-25-200000

## 起因
配筋尺寸命令与服务迁入 `Features/DimensionForReinforcement`，命名空间 `HyCADTool.Features.DimensionForReinforcement`。

## 改动
- 自 `Presentation/Commands`、`Shared/AutoCAD/Services` 移入本目录
- `ReCall/commands.json`：`dds` / `ddss` 的 `type` 更新

## 验证
- `dotnet build`；C2 后执行 dds / ddss
