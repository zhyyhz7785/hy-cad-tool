namespace HyCAD.Tables.Structure;

/// <summary>
/// 斜线方向（Diagonal Direction）。
/// </summary>
public enum DiagonalDirection
{
    /// <summary>左上到右下（↘）。</summary>
    BackSlashTLBR,

    /// <summary>左下到右上（↗）。</summary>
    SlashBLTR
}
