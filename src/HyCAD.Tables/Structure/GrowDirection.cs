namespace HyCAD.Tables.Structure;

/// <summary>
/// 表格生长方向（Grow Direction）。
/// </summary>
public enum GrowDirection
{
    /// <summary>自上而下（默认）。</summary>
    Down,

    /// <summary>自下而上（CAD 倒置表）。</summary>
    Up
}
