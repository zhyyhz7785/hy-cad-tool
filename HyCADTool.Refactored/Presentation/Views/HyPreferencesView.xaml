<UserControl x:Class="HyCADTool.Refactored.Presentation.Views.HyPreferencesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sub="clr-namespace:HyCADTool.Refactored.Presentation.Views.Preferences"
             xmlns:vm="clr-namespace:HyCADTool.Refactored.Presentation.ViewModels"
             mc:Ignorable="d"
             Background="{DynamicResource Brush_RegionBack}"
             d:DesignHeight="650" d:DesignWidth="560"
             d:DataContext="{d:DesignInstance Type=vm:HySettingsViewModel, IsDesignTimeCreatable=False}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HyCADTool.Refactored;component/Presentation/Resources/BlenderTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- ===== 一级目录 ListBoxItem 模板（Blender Preferences 风格，宽项带图标+文字） ===== -->
            <Style x:Key="PrefsSideItem" TargetType="{x:Type ListBoxItem}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource Brush_TextSecondary}"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ListBoxItem}">
                            <Grid x:Name="Root" Height="26" Background="{TemplateBinding Background}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="{Binding Icon}"
                                           FontSize="13"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Foreground="{TemplateBinding Foreground}"/>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding Name}"
                                           FontSize="12"
                                           VerticalAlignment="Center"
                                           Margin="0,0,8,0"
                                           Foreground="{TemplateBinding Foreground}"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Root" Property="Background" Value="{DynamicResource Brush_ItemHover}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource Brush_TextPrimary}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Root" Property="Background" Value="{DynamicResource Brush_ItemSelected}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource Brush_ItemSelectedText}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ===== 二级 Expander 内容：根据 GroupKey 选择具体骨架；命中则显示示例 UserControl，否则显示 TODO 占位 ===== -->
            <DataTemplate x:Key="GroupKey_StylePreview">         <sub:StyleSettingsView   Section="Preview"/></DataTemplate>
            <DataTemplate x:Key="GroupKey_TextStyle">            <sub:StyleSettingsView   Section="Text"/></DataTemplate>
            <DataTemplate x:Key="GroupKey_DimStyle">             <sub:StyleSettingsView   Section="Dim"/></DataTemplate>
            <DataTemplate x:Key="GroupKey_MLeaderStyle">         <sub:StyleSettingsView   Section="MLeader"/></DataTemplate>
            <DataTemplate x:Key="GroupKey_TableStyle">           <sub:StyleSettingsView   Section="Table"/></DataTemplate>

            <DataTemplate x:Key="GroupKey_ReinParams">           <sub:ReinSettingsView    Section="Rein"/></DataTemplate>
            <DataTemplate x:Key="GroupKey_ReinDimParams">        <sub:ReinSettingsView    Section="ReinDim"/></DataTemplate>

            <DataTemplate x:Key="GroupKey_Placeholder">
                <TextBlock Text="（待迁移：尚未接入真实参数绑定）"
                           Foreground="{DynamicResource Brush_TextDisabled}"
                           Margin="8,6"
                           FontStyle="Italic"/>
            </DataTemplate>

        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="28"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="34"/>
        </Grid.RowDefinitions>

        <!-- ===== 顶栏：面包屑 ===== -->
        <Border Grid.Row="0" Background="{DynamicResource Brush_HeaderBack}"
                BorderBrush="{DynamicResource Brush_BorderDark}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
                <TextBlock Text="⚙" FontSize="12"
                           Foreground="{DynamicResource Brush_TextSecondary}"
                           VerticalAlignment="Center"/>
                <TextBlock Text="  HyCAD 首选项"
                           FontSize="12"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource Brush_TextPrimary}"
                           VerticalAlignment="Center"/>
                <TextBlock Text="  ›  " Foreground="{DynamicResource Brush_TextDisabled}" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding SelectedCategory.Name}"
                           FontSize="12"
                           Foreground="{DynamicResource Brush_TextSecondary}"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- ===== 中部：左一级目录 + 右二级分组 ===== -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧一级目录 -->
            <Border Grid.Column="0" Background="{DynamicResource Brush_HeaderBack}"
                    BorderBrush="{DynamicResource Brush_BorderDark}" BorderThickness="0,0,1,0">
                <ListBox Style="{StaticResource BlenderListBox}"
                         ItemContainerStyle="{StaticResource PrefsSideItem}"
                         ItemsSource="{Binding Categories}"
                         SelectedItem="{Binding SelectedCategory, Mode=TwoWay}"
                         Background="Transparent"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"/>
            </Border>

            <!-- 右侧二级分组内容 -->
            <ScrollViewer Grid.Column="1"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="6,4">
                <ItemsControl ItemsSource="{Binding SelectedCategory.Groups}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:SettingsGroupVm}">
                            <Expander Style="{StaticResource BlenderExpander}"
                                      Header="{Binding Header}"
                                      IsExpanded="{Binding IsExpanded, Mode=TwoWay}"
                                      Margin="0,0,0,4">
                                <ContentControl Content="{Binding}">
                                    <ContentControl.Style>
                                        <Style TargetType="{x:Type ContentControl}">
                                            <Setter Property="ContentTemplate" Value="{StaticResource GroupKey_Placeholder}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding GroupKey}" Value="StylePreview">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource GroupKey_StylePreview}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding GroupKey}" Value="TextStyle">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource GroupKey_TextStyle}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding GroupKey}" Value="DimStyle">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource GroupKey_DimStyle}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding GroupKey}" Value="MLeaderStyle">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource GroupKey_MLeaderStyle}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding GroupKey}" Value="TableStyle">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource GroupKey_TableStyle}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding GroupKey}" Value="ReinParams">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource GroupKey_ReinParams}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding GroupKey}" Value="ReinDimParams">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource GroupKey_ReinDimParams}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ContentControl.Style>
                                </ContentControl>
                            </Expander>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <!-- ===== 底栏：5 项动作 ===== -->
        <Border Grid.Row="2" Background="{DynamicResource Brush_HeaderBack}"
                BorderBrush="{DynamicResource Brush_BorderDark}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
                    <TextBlock Text="{Binding StatusMessage}"
                               Foreground="{DynamicResource Brush_TextSecondary}"
                               FontSize="11"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="4,0,4,0">
                    <CheckBox Style="{StaticResource BlenderCheckBox}"
                              Content="自动保存"
                              VerticalAlignment="Center"
                              Margin="0,0,8,0"
                              IsChecked="{Binding AutoSaveEnabled, Mode=TwoWay}"/>
                    <Button Style="{StaticResource BlenderButton}"
                            Content="置为当前"
                            MinWidth="72" Margin="2,0"
                            Command="{Binding ApplyCurrentCommand}"/>
                    <Button Style="{StaticResource BlenderButton}"
                            Content="保存用户设置"
                            MinWidth="88" Margin="2,0"
                            Command="{Binding SaveUserSettingsCommand}"/>
                    <Button Style="{StaticResource BlenderButton}"
                            Content="恢复自动保存"
                            MinWidth="88" Margin="2,0"
                            Command="{Binding RestoreAutoSavedCommand}"/>
                    <Button Style="{StaticResource BlenderButton}"
                            Content="加载初始设置"
                            MinWidth="88" Margin="2,0"
                            Command="{Binding LoadDefaultsCommand}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
