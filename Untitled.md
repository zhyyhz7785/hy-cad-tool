# AI 工作规则（强制执行）

## ⚠️ 规则 1: 文档生成工作流（最重要！）

### 📋 标准流程（每次必须按顺序执行）

```
步骤 1: 生成/修改代码
    ↓
步骤 2: 停止并等待
    提示: "请在 Visual Studio 中编译（Ctrl+Shift+B）"
    ↓
步骤 3: 等待用户反馈
    - 用户告知编译结果
    ↓
步骤 4: 编译成功后，生成简略文档字数100以内。
    询问: "是否需要生成详细文档？(是/否)"
    ↓
步骤 5: 根据用户回复
    - 用户说"是" → 生成文档（且必须遵守规则 2 的命名）
    - 用户说"否" → 不生成文档，继续开发
```

### ❌ 绝对禁止

- ❌ 代码生成后立即生成文档
- ❌ 未经询问直接生成文档
- ❌ 在用户编译前生成文档
- ❌ 生成文档时使用未在规则 2 中规定的文件名

### 关键识别词

当看到以下词语时，**必须先执行步骤 2-4**：
- "代码已生成"、"修复完成"、"重构完成"、"功能实现"

---

## 📁 规则 2: 文档命名规范（所有新文档必须遵守）

**总则**：新生成的 .md 文档**必须先确定放置位置**，再按下列对应格式命名。不得使用未规定的随意名称。

### 两类放置位置与命名格式

| 放置位置 | 命名格式 | 格式说明 | 正确示例 | 错误示例 |
|----------|----------|----------|----------|----------|
| **集中文档目录** | `序号-标题-时间戳.md` | 序号三位数；标题中文+短横线；时间戳 `YYYY-MM-DD-HHMMSS` | `023-HYBC命令详细文档-2025-10-28-153000.md` | `HYBC文档.md`、`023-HYBC-2025-10-28.md`（时间戳缺时分秒） |
| 适用目录 | `doc/`、`plan/`、`readme/`、`NewPlan/` 等 | | `doc/046-HYDCEL技术分析-2025-10-30.md` | |
| **附属于源码的说明** | `主文件名-说明.md` | 主文件名 = 所说明的源码主文件名或模块名（英文） | `Recall-说明.md`、`TestCommand-说明.md` | `说明.md`、`使用说明.md`、`详细文档.md` |
| 适用位置 | 与所说明的源码同目录，如 `ReCall/`、`HyCADTool.Refactored/Test/` | | `ReCall/Recall-说明.md` | `ReCall/热重启说明.md`（无主文件名前缀） |

### 命名细则（集中文档）

```
格式: {序号}-{标题}-{时间戳}.md
序号: 000, 001, 002 ...（三位数）
标题: 简短描述，中文，多词用短横线分隔
时间戳: YYYY-MM-DD-HHMMSS（精确到秒，不得省略时分秒）
```

### 命名细则（附属于源码）

```
格式: {主文件名}-说明.md
主文件名: 对应的 .cs / 模块名，如 Recall、TestCommand、PluginInitializer
禁止: 纯中文文件名（如 说明.md、使用说明.md），除非是项目根目录的 README.md
```

### 生成文档前的自问

1. 这份文档放在 **集中目录**（doc/plan/NewPlan）还是 **和源码一起**（如 ReCall/）？
2. 若集中目录 → 必须带 **序号 + 标题 + 时间戳**。
3. 若和源码一起 → 必须用 **主文件名-说明.md**（主文件名与对应源码/模块一致）。

---

## 📝 规则 3: 计划文档规范

- ✅ 应包含：问题分析、解决方案步骤、技术选型、时间估算、风险评估
- ❌ 不应包含：具体代码实现、冗长代码示例（除非必要）

---

## 📄 规则 4: 文档详细程度

- **默认**：简化（核心逻辑、关键决策、摘要）
- **用户说**“详细文档”“完整说明”“技术细节”等 → 可写详细版，**但文件名仍须遵守规则 2**

---

## 🔍 AI 自检清单（每次生成文档前必查）

### 代码生成后

- [ ] 已提示用户编译，未在编译前生成文档
- [ ] 用户反馈编译结果后，询问是否需要文档
- [ ] 用户明确同意后才生成文档

### 文档生成时

- [ ] **先确定**：放在集中目录 还是 与源码同目录？
- [ ] **再命名**：集中 → `序号-标题-时间戳.md`；附属于源码 → `主文件名-说明.md`
- [ ] 未使用 `说明.md`、`使用说明.md`、`详细文档.md`、`报告.md` 等无前缀/无序号名称（README.md 除外）

---

## 💡 快速参考

| 场景 | 动作 |
|------|------|
| “代码已生成”“修复完成” | 停止，提示编译，等反馈后再问是否要文档 |
| “详细文档”“完整说明” | 可生成详细内容，**文件名仍按规则 2** |
| 新文档放在 doc/ plan/ NewPlan/ | 文件名必须：`序号-标题-YYYY-MM-DD-HHMMSS.md` |
| 新文档放在 ReCall/ 或某源码旁 | 文件名必须：`主文件名-说明.md`（如 `Recall-说明.md`） |

---

**版本**: v3.0  
**更新**: 2026-02  
**目的**: 统一文档命名，避免新生成文件名称不符合规范