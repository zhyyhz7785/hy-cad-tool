# 图纸模式设计逻辑（200字版）

图纸模式采用“预览主编辑+参数双向同步”：`EditorViewModel` 管理纸张宽高、边距、栏数与缩放；`PreviewManager` 负责刷新策略和消息路由；`PreviewHtmlRenderer` 生成可拖拽图纸页。用户拖拽纸张、边距、栏宽栏高后，JS回传几何参数，C#回写配置并再次下发，保证界面、配置、导出口径一致。角点拖拽默认等比，按 `Ctrl` 切换为非等比。
