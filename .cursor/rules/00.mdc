# AI 工作规则（强制执行）

## ⚠️ 规则 1: 文档生成工作流（最重要！）

### 📋 标准流程（每次必须按顺序执行）

```
步骤 1: 生成/修改代码
    ↓
步骤 2: 停止并等待
    提示: "请在 Visual Studio 中编译（Ctrl+Shift+B）"
    ↓
步骤 3: 等待用户反馈
    - 用户告知编译结果
    ↓
步骤 4: 编译成功后，询问文档需求
    问题: "是否需要生成详细文档？(是/否)"
    ↓
步骤 5: 根据用户回复
    - 用户说"是" → 生成文档
    - 用户说"否" → 不生成文档，继续开发
```

### ❌ 绝对禁止（违反此规则将被标记错误）

- ❌ 代码生成后立即生成文档
- ❌ 未经询问直接生成文档
- ❌ 在用户编译前生成文档
- ❌ 生成多个文档（除非用户明确要求"详细"）

### ✅ 正确示例

```
AI: "圆分割问题已修复！请在 Visual Studio 中编译（Ctrl+Shift+B）"
用户: "编译成功"
AI: "是否需要生成详细文档？"
用户: "是" 
AI: 生成文档 ✅
```

### 关键识别词

当看到以下词语时，**必须先执行步骤2-4**：
- "代码已生成"
- "修复完成"
- "重构完成"
- "功能实现"

---

## 📁 规则 2: 文档目录规范

### 文档分类和命名

| 文档类型 | 目录 | 命名格式 | 示例 |
|---------|------|---------|------|
| **总结文档** | `/doc/` | `序号-标题-时间戳.md` | `000-HYBC修复报告-2025-10-28-193045.md` |
| **计划文档** | `/plan/` | `序号-标题-时间戳.md` | `001-重构计划-2025-10-28-120000.md` |
| **说明文档** | `/readme/` | `序号-标题-时间戳.md` | `002-使用说明-2025-10-28-150000.md` |

### 命名规则细则

```
格式: {序号}-{标题}-{时间戳}.md

序号: 000, 001, 002... (三位数，按生成顺序递增)
标题: 简短描述（中文，用短横线分隔）
时间戳: YYYY-MM-DD-HHMMSS (精确到秒)

示例: 
✅ 023-HYBC命令详细文档-2025-10-28-153000.md
❌ HYBC文档.md (缺少序号和时间戳)
❌ 023-HYBC-2025-10-28.md (时间戳不精确)
```

---

## 📝 规则 3: 计划文档规范

### 计划文档内容要求

✅ **应该包含**：
- 问题分析
- 解决方案（步骤）
- 技术选型
- 时间估算
- 风险评估

❌ **不应该包含**：
- 具体代码实现
- 代码示例（除非必要说明）
- 详细 API 调用

### 示例对比

❌ **错误（包含代码）**：
```markdown
## 步骤 1: 创建服务类
```csharp
public class MyService 
{
    public void DoSomething() { ... }
}
```

✅ **正确（只描述逻辑）**：
```markdown
## 步骤 1: 创建服务类
- 职责: 处理业务逻辑
- 依赖: IRepository 接口
- 方法: Execute() - 执行核心流程
```

---

## 📄 规则 4: 文档详细程度控制

### 默认模式：简化文档

✅ **只保留**：
- 核心逻辑（3-5 句话）
- 关键设计决策
- 重要的架构变更
- 问题和解决方案摘要

❌ **省略**：
- 详细代码示例
- 逐行代码解释
- 完整 API 文档
- 历史记录和背景

### 触发详细模式的关键词

**用户说以下词语 → 生成详细文档**：
- "详细文档"
- "完整说明"
- "技术细节"
- "包含代码示例"
- "API 文档"

### 示例对比

#### 简化模式（默认）
```markdown
## 圆分割修复
- 问题: 参数未排序导致死机
- 解决: 添加 Distinct() 和 OrderBy()
- 结果: 测试通过
```

#### 详细模式（用户要求）
```markdown
## 圆分割修复详细说明

### 问题分析
1. 原因: AutoCAD API GetSplitCurves() 要求...
2. 场景: Polyline 与 Circle 有重复交点...

### 技术实现
```csharp
var sortedParams = item.Value
    .Distinct()
    .OrderBy(p => p)
    .ToList();
```

### 测试验证
...
```

---

## 🔍 AI 自检清单（每次响应前检查）

### 代码生成场景

- [ ] 代码已生成/修改
- [ ] 已提示用户编译（**不要继续其他操作**）
- [ ] 用户反馈编译结果
- [ ] 编译成功 → 询问是否需要文档
- [ ] 用户明确同意 → 才生成文档

### 文档生成场景

- [ ] 用户已明确同意生成文档
- [ ] 文档类型正确（doc/plan/readme）
- [ ] 文件名符合规范（序号-标题-时间戳）
- [ ] 详细程度符合要求（默认简化，除非用户要求详细）

---

## 💡 快速参考卡

### 看到这些 → 停止并等待编译

```
"代码已生成"
"修复完成"  
"重构完成"
"功能已实现"
```

### 看到这些 → 生成详细文档

```
"详细文档"
"完整说明"
"技术细节"
```

### 看到这些 → 生成简化文档

```
"总结一下"
"简单说明"
（默认模式）
```

---

**版本**: v2.0  
**更新**: 2025-10-28  
**目的**: 让 AI 更容易理解和遵守规则
