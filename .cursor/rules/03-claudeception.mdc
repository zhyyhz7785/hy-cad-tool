---
description: 持续学习系统——任务完成后自动评估是否有可提炼的经验，保存为 Cursor Skill
alwaysApply: true
---
# Claudeception (Cursor 适配)

完成每个**有实质性调查/调试/试错**的任务后，执行以下评估：

## 触发条件（满足任一即触发）

1. 调试耗时且解法不在文档中
2. 错误信息有误导性，根因非显而易见
3. 试了多种方案才找到可行解
4. 发现了项目/框架特有的配置或模式
5. 用户显式要求：`/claudeception`、"保存为技能"、"我们学到了什么"

## 评估协议

```
完成用户请求 → 自问三个问题：
  ① 这次需要非显而易见的调查吗？
  ② 这个方案对未来类似问题有帮助吗？
  ③ 纯看文档能直接得到答案吗？
→ 任一为"是" → 提炼 Skill
→ 全部为"否" → 跳过
```

## 提炼流程

1. **查重**：搜索 `.cursor/skills/` 下是否已有同类 Skill
2. **结构化**：按下方模板写 `SKILL.md`
3. **保存**：写入 `.cursor/skills/<skill-name>/SKILL.md`
4. **告知用户**：简要说明提炼了什么、为什么值得保留

## Skill 模板

```markdown
---
name: descriptive-kebab-case-name
description: |
  精确描述：(1) 解决什么问题 (2) 触发条件/错误信息 (3) 涉及的技术栈
author: Cursor Agent
version: 1.0.0
date: YYYY-MM-DD
---
# 标题
## Problem
## Context / Trigger Conditions
## Solution
## Verification
## Notes
```

## 质量门槛

- 可复用（不只是本次有效）
- 非平凡（不是文档直接能查到的）
- 具体（有明确触发条件和解决步骤）
- 已验证（方案确实有效）
- 不含敏感信息

## 不提炼的情况

- 纯文档查询、简单配置、一步搞定的任务
- 无法泛化的一次性操作
