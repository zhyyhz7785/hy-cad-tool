---
alwaysApply: true
---
# 01-AI热启动模式 - 新对话必读

> **用途**：项目状态与操作规范  
> **更新**：2026-02

---

## 一、做测试时只用这一个文件

**要测哪个命令，只改这个文件里的一行：**

`HyCADTool.Refactored/Test/TestCommand.cs`

```csharp
// 只改下面这一行即可切换测试命令
new Presentation.Commands.OverKillCommand().Execute();
// 例如：new Presentation.Commands.BreakCurvesCommand().Execute();
```

- **C2**：重载插件（从副本加载，不锁 bin\Debug，可不关 AutoCAD 重新生成）
- **C1**：执行上面配置的那一个命令（带 SimpleLogger 耗时）

不要改 ReCall、不要改 TestRunner；换测命令只改 TestCommand.cs。

### 当前双文件约定（必须遵守）

- `HyCADTool.Refactored/Test/TestCommand.cs`：Refactored 实测入口，默认“一键打开统一面板”用于联调。
- `HyCADTool.Refactored/Test/TestCommand - 复制.cs`：仅作为 MarkdownEditor 文档测试参考模板，保持注释态，不作为 Refactored 编译入口。
- 当测试 Refactored 面板时，只执行 `C2 -> C1`，并且只改 `TestCommand.cs` 一行命令。

---

## 二、开发循环

```
改代码 → VS 编译 → C2 → C1 → 重复
```

- 编译由用户在 Visual Studio 完成，完成后用户反馈
- 常用命令输出：极简 1–3 行；循环内禁止输出

---

## 三、关键文件

| 文件 | 用途 |
|------|------|
| `HyCADTool.Refactored/Test/TestCommand.cs` | **测试入口**：只改此处切换 C1 测谁 |
| `HyCADTool.Refactored/Test/SimpleLogger.cs` | 耗时输出（LogElapsedTime） |
| `HyCADTool.Refactored/Presentation/Commands/*` | 命令实现（主要工作区） |
| `ReCall/Recall.cs` | 热重启 C2/C1，仅可改配置区 |

---

## 四、必须遵守

1. **极简输出**：常用命令 1–3 行
2. **循环内禁止输出**
3. **只改 Refactored**：ReCall 仅改配置；旧项目 HyCADtool 不修改
4. **异常**：引用 AutoCAD 的 C# 里用 `System.Exception`（见 06 规则）
5. **代码完成后必须更新测试入口**：每次完成命令/面板代码后，修改 `TestCommand.cs` 的 `Run()` 方法指向新完成的命令，确保用户可以 C2→C1 立即测试

---

## 五、架构（快速）

- Domain：平台无关，可迁 Blender  
- Infrastructure：AutoCAD 封装，依赖 Domain  
- Presentation：命令与 UI，依赖 Domain + Infrastructure  

---

## 六、当前工作状态（2026-02-10）

### 已完成

**HY 设置面板** — SettingsPanel + SettingsPanelViewModel + StyleService + TextBoxHelper

**钢筋命令（全部完成）**：
- `DrawReinforcementCommand` (gj)、`DrawOffsetPolylineCommand` (gg)
- `ReinAddAnchorCommand` (g1/g2)、`ReinCutCommand` (gd)
- `ReinExtendCommand` (ge)、`ReinQuickExtendCommand` (ge1)
- `MleaderReinCommand` (gb/gb1/gb2)
- `ReinService` — 绘制服务（除 `DimensionRein` 外完成）

**底板配筋（BaseRein）**：
- `BaseReinforcementConfig` — 完整配置模型
- `BaseReinPanel.xaml` + `BaseReinPanelViewModel` — 完整面板
- `BaseReinforcementService` — 全部 6 个步骤已实现：
  - Step 1 整理底图、Step 2 选择删除文字
  - Step 4 生成配筋面积（网格选择→包络→分组→合并）
  - Step 5 绘制钢筋（单方向/全方向 + 标注文字）
  - Step 6 标注配筋区域

**架构纠偏（024 文档三轮修复）**：Domain 100% 纯净，DI 统一，废弃代码清理

### 已完成（2026-02-11）

**标高命令（hybg* → 4 命令）**：
- `DrawElevationCommand` (bg)、`UpdateElevationTextCommand` (bgu)
- `RotateElevationCommand` (bgR)、`TextsCreateElevationCommand` (hybgTCE)
- `ElevationSymbolJig`（Jig）、`ElevationService`（服务）、`ElevationSymbolState`（枚举）

**尺寸标注（hydim* → 2 独立命令）**：
- `AddVertexAtIntersectionsCommand` (hydimA)
- `SplitDimensionCommand` (sd)
- 注：`DimensionForReinforcement (dds/ddss)` 依赖链深（884+769行），待独立迁移

**地脚螺栓（hyab* → 6 命令）**：
- `AnchorBoltCommand` (hyab)、`AnchorBoltAlignCommand` (hyabA)
- `AnchorBoltCalcCommand` (hyabC)、`AnchorBoltToggleDisplayCommand` (hyabCD)
- `AnchorBoltTableCommand` (hyabCT)、`AnchorBoltSectionCommand` (hyabR)
- `AnchorBolt` + `AnchorBoltFactory`（Domain）、`ExtensionDictionaryService`（Infrastructure）

**尺寸标注 DimensionForReinforcement（dds/ddss → 2 命令）**：
- `DimensionForReinforcementCommand` (dds)、`DimensionForReinforcementBatchCommand` (ddss)
- `DimensionForReinforcementService`（780行，完整迁移）

**设备基础（hyef* → 6 命令）**：
- `EF_ConstructBaseDataCommand` (hyef_Base_ConstructBaseData)
- `EF_HighlightBoltDataCommand` (hyef_Base_HighlightBoltData)
- `EF_AxisConstructCommand` (hyef_Axis_Construct)
- `EF_AxisInitializeCommand` (hyef_Axis_Initialize)
- `EF_AxisDisplayCommand` (hyef_Axis_Display)
- `EF_AxisCreateTableCommand` (hyef_Axis_CreateTable)
- Domain: `BaseData` + `BoltData` + `AxisData` + `EquipmentData` + `EquipmentDataManager`
- Infrastructure: `EquipmentFoundationService` + `ExtensionDictionaryService`（Guid/BaseData/BoltData/AxisData 读写）

### 进行中：其他命令迁移

按优先级排序：
1. ~~标高命令 hybg*~~ ✓
2. ~~尺寸标注 hydim* 独立命令~~ ✓
3. ~~地脚螺栓 hyab*~~ ✓
4. ~~尺寸标注 DimensionForReinforcement (dds/ddss)~~ ✓
5. ~~设备基础 hyef*~~ ✓
6. 其他杂项（导出、轴线文字、筏板厚度、块操作等）

### 坐标与单位规则

- **模型空间坐标 = 实际 mm（1:1）**
- 面板参数分两类：

**直接用（实际 mm，红色）**：不乘 Scale
| 参数 | 默认 | 说明 |
|------|------|------|
| `RebarDiameter` | 14 | 钢筋直径，如 15d = 15×14 = 210mm |
| `AnchorageLength` | 500 | 锚固长度 |
| `DotSeparation` | 200 | 点钢筋间距 |
| `BendingLineMinLength` | 150 | 弯折最小长度 |
| `AnchorageJoinLength` | 1500 | 锚固连接长度 |
| `RebarSpacing` | 200 | 钢筋间距 |

**需 × Scale（缩放值，绿色）**：面板存的是 `实际mm / Scale`，用于几何时需 `值 × Scale`
| 参数 | 默认 | ×40 实际 | 说明 |
|------|------|----------|------|
| `ProtectionThickness` | 1.0 | 40mm | 保护层厚度 |
| `HookLength` | 1.0 | 40mm | 弯钩长度 |
| `ReinforcementDiameter` | 0.35 | 14mm | 点钢筋绘制直径 |
| `DotReinOffset` | 1.35 | 54mm | 点钢筋偏移 |

- **Scale 还用于**：文字/标注/引线样式符号大小（`TextSize × Scale`、`Dimscale`）

### 关键约束

- 重构项目中 **不能用 `[CommandMethod]`**（热重载会 eDuplicateKey）
- 面板用 `ShowPanelCommand.ShowSettingsPanel()`（无 CommandMethod），通过 TestCommand 调用
- 旧代码的 `ReinPanel.ActivePanel?.xxx` 改为 `SettingsPanelViewModel.Current?.xxx`
- 旧代码的 `BaseConfig.xxx` 改为从 SettingsPanelViewModel 读取
- 旧代码的 `ZTools.CreateSolidCircle` 改为 `EntityExtensions.CreateSolidCircle`
- 旧代码的 `.ToSpace()` 改为 `EntityExtensions.ToSpace()`
- 旧代码的 `ZTools.AddMleader` 改为 `MLeaderExtensions.AddMleader`

---

**版本**：v5.0
