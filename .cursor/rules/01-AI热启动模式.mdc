---
alwaysApply: true
---
# 01-AI热启动模式 - 新对话必读

> **用途**：项目状态与操作规范  
> **更新**：2026-06（编译改为 AI 主导；主工程已转正为 `src/HyCADTool`）

---

## 一、做测试时只用这一个文件

**要测哪个命令，只改这个文件里的一行：**

`src/HyCADTool/App/Test/TestCommand.cs`（主工程 **HyCADTool**）

```csharp
// 只改下面这一行即可切换测试目标（在 TestCommand.Run 内 SimpleLogger 回调中）
// 例如调用 Shell 内 ShowPanelCommand 或 Features 下某命令的 .Execute()
ShowPanelCommand.ShowHyBlenderPanel();
```

- **C2**：重载插件（从副本加载，不锁 bin\Debug，可不关 AutoCAD 重新生成）
- **C1**：执行上面配置的那一个命令（带 SimpleLogger 耗时）

不要改 ReCall、不要改 TestRunner；换测命令只改 TestCommand.cs。

### 基础沉降（固定命令，不必用 C1）

在命令行直接输入 **`HYJC`** 或 **`hySC`** 即可打开「基础沉降计算」独立窗口（与 C1 联调无关）。C1 仅用于开发时从 `TestCommand.cs` 一键触发任意命令。

### 当前约定（必须遵守）

- `src/HyCADTool/App/Test/TestCommand.cs`：C1 联调入口；默认可一键打开统一面板（`ShowHyBlenderPanel`）。
- 换测时只改 `TestCommand.Run()` 内一行，**不要**改 `ReCall`、`TestRunner`（除非排障约定）。
- 当测试统一面板时，只执行 `C2 -> C1`。

---

## 二、开发循环

```
改代码 → AI 编译（dotnet build） → 用户在 AutoCAD 输 C2 → C1 → 重复
```

- **编译由 AI 完成**，不再让用户去 VS 编译：

```powershell
dotnet build src\HyCADTool\HyCADTool.csproj -c Debug
```

- 编译失败 AI 自己修到 0 error；`CS2012 被 MSBuild.exe 锁定` → `dotnet build-server shutdown` 后加 `-nodeReuse:false` 重试
- C2 / C1 只能在 AutoCAD 命令行执行，AI 编译通过后**提示用户**去执行
- 常用命令输出：极简 1–3 行；循环内禁止输出

### 编译与冷启动边界

| 改了什么 | 谁编译 | AutoCAD 要不要关 |
|---|---|---|
| `src/HyCADTool/**`（业务代码/XAML） | **AI** `dotnet build` 主工程 | 不用关，C2 热重载 |
| `src/HyCAD.BlenderUI/**` 等被主工程引用的项目 | **AI** `dotnet build` 主工程（连带编译） | 不用关，C2 热重载 |
| `src/ReCall/commands.json` | 不需编译，`Copy-Item` 到 `ReCall\bin\Debug` | 不用关 |
| `src/ReCall/*.cs` | **冷启动：提醒用户**关 AutoCAD → 编译 ReCall → 重启并 NETLOAD | **必须关** |

主工程已与 ReCall 完全解耦（不引用 ReCall.dll），AutoCAD 开着时 AI 编译主工程不会碰锁文件。**禁止**在 AutoCAD 开着时 build 整个 .sln（会因 ReCall.dll 被锁而失败）。

---

## 三、关键文件

| 文件 | 用途 |
|------|------|
| `src/HyCADTool/App/Test/TestCommand.cs` | **测试入口**：只改此处切换 C1 测谁 |
| `src/HyCADTool/App/Bootstrap/SimpleLogger.cs` | 耗时输出（LogElapsedTime） |
| `src/HyCADTool/Features/*` | 业务命令与面板（主要工作区） |
| `src/HyCADTool/Shell/*` | 统一面板 / 首选项 / Ribbon / 命令分发 |
| `src/ReCall/Recall.cs` | 热重启 C2/C1，仅可改配置区 |

---

## 四、必须遵守

1. **极简输出**：常用命令 1–3 行
2. **循环内禁止输出**
3. **主工程 `src/HyCADTool`**：ReCall 仅改配置；旧项目 `HyCADtool` 不修改
4. **异常**：引用 AutoCAD 的 C# 里用 `System.Exception`（见 06 规则）
5. **代码完成后必须更新测试入口**：每次完成命令/面板代码后，修改 `TestCommand.cs` 的 `Run()` 方法指向新完成的命令，确保用户可以 C2→C1 立即测试
6. **改完代码必须 AI 编译通过再交差**：`dotnet build src\HyCADTool\HyCADTool.csproj -c Debug` 0 error 后才提示用户 C2→C1；只有改 `src/ReCall/*.cs` 时才提醒用户冷启动（见第二节边界表）

---

## 五、架构（快速）

- Domain / Shared.Geometry：平台无关算法与模型  
- Shared.AutoCAD：AutoCAD 侧通用封装（图层、选集、部分服务）  
- Features：按业务切片的命令 + 面板 + 领域子集  
- Shell：侧栏/首选项/Ribbon/命令基础设施  
- App：插件启动、Autofac、生产入口、TestCommand  
- Infrastructure：命令表 JSON 镜像等（逐步收拢到 Features / Road）  

---

## 六、项目现状（2026-06）

- **主工程已转正**：`HyCADTool.Refactored` 改名为 **`HyCADTool`**（`src/HyCADTool`，`AssemblyName=HyCADTool`），源码中已无任何 `Refactored` 引用；旧文档/skill 里的 `HyCADTool.Refactored` 均指现在的主工程
- **命令迁移已完成**：钢筋(gj/gb*)、标高(bg*)、尺寸标注(dds/ndds/sd)、地脚螺栓(hyab*)、设备基础(hyef*)、图框视口、道路(Alignment/CrossSection/Profile/Intersection)、文字编辑(hyed) 等全部走 `commands.json` 注册；命令清单以 `src/ReCall/commands.json` 为唯一真相源
- **命令类命名空间**：`HyCADTool.Features.<域>.*`（命令+面板+领域子集）与 `HyCADTool.Shell.Commands.*`（面板/基础设施）
- **HyCADTool 与 ReCall 已完全解耦**（2026-04-25）：主工程不引用 ReCall.dll，生产模式 `dotnet build -c Production` 后直接 NETLOAD HyCADTool.dll；开发模式仍由 ReCall C2 热重载
- 当前活跃工作区：`Features/Road/*`（道路设计）、`Features/Reinforcement`（构件识别）、`HYFEA`（有限元）

### 坐标与单位规则

- **模型空间坐标 = 实际 mm（1:1）**
- 面板参数分两类：

**直接用（实际 mm，红色）**：不乘 Scale
| 参数 | 默认 | 说明 |
|------|------|------|
| `RebarDiameter` | 14 | 钢筋直径，如 15d = 15×14 = 210mm |
| `AnchorageLength` | 500 | 锚固长度 |
| `DotSeparation` | 200 | 点钢筋间距 |
| `BendingLineMinLength` | 150 | 弯折最小长度 |
| `AnchorageJoinLength` | 1500 | 锚固连接长度 |
| `RebarSpacing` | 200 | 钢筋间距 |

**需 × Scale（缩放值，绿色）**：面板存的是 `实际mm / Scale`，用于几何时需 `值 × Scale`
| 参数 | 默认 | ×40 实际 | 说明 |
|------|------|----------|------|
| `ProtectionThickness` | 1.0 | 40mm | 保护层厚度 |
| `HookLength` | 1.0 | 40mm | 弯钩长度 |
| `ReinforcementDiameter` | 0.35 | 14mm | 点钢筋绘制直径 |
| `DotReinOffset` | 1.35 | 54mm | 点钢筋偏移 |

- **Scale 还用于**：文字/标注/引线样式符号大小（`TextSize × Scale`、`Dimscale`）

### 关键约束

- 主工程中 **不能用 `[CommandMethod]`**（热重载会 eDuplicateKey；生产入口的 `Production/` 文件夹除外，由 `HYCAD_PRODUCTION` 常量隔离）
- 面板用 `HyCADTool.Shell.Commands.ShowPanelCommand.ShowSettingsPanel()`（无 CommandMethod），通过 TestCommand 调用；类文件在 `Shell/Commands/ShowPanelCommand.cs`
- 旧代码的 `ReinPanel.ActivePanel?.xxx` 改为 `SettingsPanelViewModel.Current?.xxx`
- 旧代码的 `BaseConfig.xxx` 改为从 SettingsPanelViewModel 读取
- 旧代码的 `ZTools.CreateSolidCircle` 改为 `EntityExtensions.CreateSolidCircle`
- 旧代码的 `.ToSpace()` 改为 `EntityExtensions.ToSpace()`
- 旧代码的 `ZTools.AddMleader` 改为 `MLeaderExtensions.AddMleader`

---

**版本**：v6.0（2026-06：AI 主导编译；删除 2026-02 命令迁移流水账，换为项目现状锚点）
