---
alwaysApply: true
---
# AI 工作规则（强制）

## 规则 0: 输出规范

- **节约 token**：回答直击要点，不重复问题、不铺垫、不总结已知信息
- **格式统一**：代码用代码块，列表用列表，不混用；同类信息用相同格式
- **禁止冗余**：不输出"让我来看看""好的我理解了"等无信息量语句；不逐行解释显而易见的代码

---

## 规则 1: 代码→AI编译→文档 流程

```
改代码 → AI 自己编译验证 → 通过后提示用户 C2→C1 测试 → 成功后问"需要文档吗？" → 用户同意才生成
```

**AI 编译（默认，AutoCAD 开着也安全）**：

```powershell
dotnet build src\HyCADTool\HyCADTool.csproj -c Debug
```

- 主工程已与 ReCall 完全解耦，编译不会触碰被 AutoCAD 锁定的 `ReCall.dll`
- 编译失败时 AI 自己读错误、修复、重编，直到 0 error，不要把编译错误丢给用户
- 若报 `CS2012 obj\...dll 被 MSBuild.exe 锁定`：先 `dotnet build-server shutdown` 再加 `-nodeReuse:false` 重试

**唯一例外（冷启动，必须提醒用户）**：改了 `src/ReCall/*.cs` → AI 改完后提醒用户「关闭 AutoCAD → 编译 ReCall → 重启 AutoCAD 并 NETLOAD」，AI 不要在 AutoCAD 开着时编译 ReCall

**禁止**：代码完成后直接生成文档、编译前生成文档、未经询问生成文档、改完代码不编译就交差

---

## 规则 2: 文档命名

| 位置 | 格式 | 示例 |
|------|------|------|
| `doc/` `plan/` `NewPlan/` | `序号-标题-YYYY-MM-DD-HHMMSS.md` | `023-HYBC修复-2025-10-28-153000.md` |
| 源码同目录 | `主文件名-说明.md` | `Recall-说明.md` |

**禁止**：`说明.md`、`使用说明.md`、无序号/无时间戳的集中文档名

---

## 规则 3: 文档内容

- **默认简化**：核心逻辑 + 关键决策 + 结果摘要
- **详细模式**：仅当用户说"详细文档""完整说明""技术细节"时触发
- **计划文档**：只描述方案步骤，不写代码实现

---

## 规则 4: 重构原则
- 遵循 Clean Architecture + DDD + SOLID
- 警惕过度工程化与边界泄漏

- **禁止反射桥接**：重构旧代码时，必须将逻辑真正移植到主工程 `src/HyCADTool`，不得用反射调用旧项目
- **分段重构**：当重构范围超过 3 个文件或 300 行时，主动提示"建议分段"，列出阶段计划，逐段执行
- **分层移植**：遵循 Domain → Infrastructure → Presentation 顺序
  - Domain：纯算法/数据结构，平台无关（不引用 AutoCAD API）
  - Infrastructure：AutoCAD 绘图/转换，依赖 Domain
  - Presentation：命令/面板，编排 Domain + Infrastructure
- **复用已有**：优先使用主工程已有的实体/枚举/值对象，不重新定义

---

**版本**: v6.0 | **更新**: 2026-06（编译改为 AI 主导 dotnet build；仅改 ReCall 才冷启动）
