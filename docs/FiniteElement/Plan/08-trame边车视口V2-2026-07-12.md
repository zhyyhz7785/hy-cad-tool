# trame 边车视口 V2（008）

> 文档日期：2026-07-12  
> 性质：008 V2 实施结案（代码已落地）  
> 上承：[Research/008](../Research/008-开源后处理整包复用与许可安全选型-2026-07-11.md)、[Plan/07](07-vtkjs内嵌视口PoC-2026-07-11.md)

---

## §0 执行摘要

| 项 | 内容 |
|---|---|
| 目标 | 云图 + 变形前后叠加 + 网格边 + 探针 + 变形动画 |
| 技术 | PyVista + trame Python 边车 → Shell WebView2 |
| 数据 | `%TEMP%\hyfea-last.vtu`（含向量场 `U`） |
| 兜底 | 边车失败 → 现有 vtk.js 本地页 |

---

## §1 目录

```text
src/HYFEA/
├── HYFEA.Viz/                 # PointVectors + VTU U 场
├── HYFEA.Viewer.Py/           # 新增边车
│   ├── viewer.py
│   ├── requirements.txt
│   ├── README.md
│   └── .venv/                 # 本机创建，不入库
└── HYFEA.Shell/
    ├── ViewerSidecar.cs
    └── MainWindow.axaml.cs    # 优先边车，失败回落 vtk.js
```

---

## §2 首次准备边车

```powershell
cd src\HYFEA\HYFEA.Viewer.Py
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

---

## §3 验收清单

| # | 标准 | 状态 |
|---|---|---|
| 1 | `dotnet test` 全绿（含 `U` 向量场） | 通过 |
| 2 | `dotnet build` Shell 0 error | 通过 |
| 3 | 边车 HTTP 冒烟（本地 200） | 通过 |
| 4 | Shell：求解 → 视口变形叠加云图 | 待目视 |
| 5 | 无 venv 时回落 vtk.js | 代码已实现 |

启动：

```powershell
dotnet run --project src\HYFEA\HYFEA.Shell\HYFEA.Shell.csproj -c Debug
```

---

## §4 操作提示

- 工具栏：**标量** / **变形比例** / **网格边** / **原网格** / **动画**
- 点击网格点 → 底部探针 chip 显示 UX/UY/|U|
- 再求解会改写 VTU；边车按 mtime 约 1s 内刷新

---

## §5 明确延后

Python 运行时打包、Gmsh 网格生成、切片/流线。

---

**版本**：v1.0 | **更新**：2026-07-12
