# 阶段 1.5 — Avalonia 独立壳 PoC 与宿主抽象

> 文档日期：2026-07-11  
> 性质：阶段 1.5 实施结案（代码已落地）  
> 上承：[Research/006](../Research/006-HYFEA独立现代UI外壳选型-替换WPF与预留AutoCAD桥接-2026-07-11.md)

---

## §0 执行摘要

| 项 | 内容 |
|---|---|
| 目标 | 不开 AutoCAD，完成「改梁元参数 → 求解 → 结果表」 |
| 框架 | Avalonia 12.1 + Fluent Dark |
| 抽象层 | `HYFEA.Hosting`（netstandard2.0） |
| UI | `HYFEA.Shell`（net8.0-windows） |
| CAD | N7 / BeamMvpPanel **保留**；`HyfeaBeamProblemBuilder` 薄包装至 Factory |

---

## §1 目录树

```text
src/HYFEA/
├── HYFEA.Core/          # 不变
├── HYFEA.Hosting/       # 新增：接口 + BeamProblemFactory + Session
│   ├── IFemHostGeometry.cs
│   ├── IFemResultSink.cs
│   ├── BeamSupportKind.cs
│   ├── BeamSolveRequest.cs
│   ├── BeamProblemFactory.cs
│   ├── InMemoryAxisGeometry.cs
│   └── BeamSolveSession.cs
├── HYFEA.Shell/         # 新增：Avalonia 独立壳
│   ├── ViewModels/MainViewModel.cs
│   └── MainWindow.axaml
└── HYFEA.Tests/
    └── Integration/BeamSolveSessionTests.cs
```

---

## §2 验收清单

| # | 标准 | 状态 |
|---|---|---|
| 1 | `dotnet test` HYFEA.Tests 全绿（含 BeamSolveSession） | 通过（25） |
| 2 | `dotnet build` HYFEA.Shell 0 error | 通过 |
| 3 | `dotnet build` HyCADTool 0 error（Hosting 引用） | 通过 |
| 4 | Shell 不引用 HyCADTool / AutoCAD | 通过 |
| 5 | Core 无 UI 引用 | 通过 |
| 6 | 改 EndA/EndB / q → 求解 → 结果表（目视） | 待用户 `dotnet run` |

启动：

```powershell
dotnet run --project src\HYFEA\HYFEA.Shell\HYFEA.Shell.csproj -c Debug
```

---

## §3 宿主接口（可编译）

| 类型 | 职责 |
|---|---|
| `IFemHostGeometry` | `GetAxisNodes()` |
| `IFemResultSink` | `Present(FemResult, FemProblem)` |
| `BeamSolveSession` | 几何 + Request → Factory → LinearStaticAnalysis → Sink |
| `InMemoryAxisGeometry` | 水平梁假几何（Shell 默认） |

**远期 CAD**：`HyfeaGeometryMapper` 实现 `IFemHostGeometry`；`BeamResultRenderer` 适配 `IFemResultSink`（本包未做）。

---

## §4 风险与后续

| 风险 | 对策 |
|---|---|
| 双壳字段漂移 | `BeamSolveRequest` / Factory 共用；WPF 面板仍用本地属性 + 薄包装 |
| NumericUpDown 绑定精度 | PoC 够用；必要时改 TextBox |

**下一包**：AutoCAD 实现宿主接口；Shell V1 加 2D 示意 + 主题细化。

---

**版本**：v1.0 | **更新**：2026-07-11
