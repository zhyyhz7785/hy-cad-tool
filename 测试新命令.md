# 测试新命令步骤

## 准备工作
1. 在 Visual Studio 中编译 `HyCADTool.Refactored` 项目
2. 确保编译成功，无错误

---

## 测试 1：圆分组标注命令

### 准备测试数据
1. 打开 AutoCAD
2. 绘制若干圆（例如 10 个圆）
3. 在每个圆附近添加文字（TEXT 或 MTEXT），内容为标高数字，例如：
   - `-1.500`
   - `-1.200`
   - `-0.800`
   - `-0.500`
   - `0.000`

### 执行测试
1. 在 AutoCAD 命令行输入 **C2**（重载插件）
2. 输入 **C1**（打开桩面板）
3. 点击 **圆分组标注** 按钮
4. 命令行提示：`是否在引线中标注标高？ [是(Y)/否(N)]:`
   - 输入 `Y` 或 `N`
5. 命令行提示：`选择对象:`
   - 框选所有圆和文字
   - 按回车确认

### 预期结果
- ✅ 圆按标高分组到不同图层（`00_hy_Z_A`, `00_hy_Z_B`, ...）
- ✅ 图层颜色渐变（蓝色 → 红色）
- ✅ 每个圆添加引线标注（编号 A1, A2, B1, B2, ...）
- ✅ 如果选择标注标高，引线显示：`A1\P标高 = -1.500`
- ✅ 生成统计表格（代号、标高、数量、总计）
- ✅ 命令行输出：`圆分组标注完成，共 X 个圆。`

---

## 测试 2：桩位 Voronoi 优化（自动生成初始点）

### 准备测试数据
1. 打开 AutoCAD
2. 绘制一个闭合多段线（PLINE），作为桩布置区域
   - 例如：矩形 10000mm × 8000mm

### 执行测试
1. 在 AutoCAD 命令行输入 **C2**（重载插件）
2. 输入 **C1**（打开桩面板）
3. 点击 **桩位Voronoi优化** 按钮
4. 命令行提示：`请选择一个闭合的多段线:`
   - 点击刚才绘制的多段线
5. 命令行提示：`请输入桩的根数 (0表示自动计算):`
   - 输入 `50`（或 `0` 自动计算）
6. 命令行提示：`请输入桩直径(单位mm):`
   - 输入 `400`
7. 命令行提示：`请输入目标置换率 (0-100)%:`
   - 输入 `2.2`
8. 等待优化完成（约 10-30 秒）

### 预期结果
- ✅ 图层 `00-HY-桩` 中绘制 50 个圆（桩）
- ✅ 图层 `00-HY-Voronoi` 中绘制 Voronoi 区域（多边形）
- ✅ 桩位分布均匀，Voronoi 单元大小接近
- ✅ 命令行输出：`桩布置完成，共 50 根桩，置换率: 2.20%。`

---

## 测试 3：桩位优化（基于已有圆）

### 准备测试数据
1. 打开 AutoCAD
2. 绘制一个闭合多段线（PLINE），作为桩布置区域
3. 在多段线内绘制若干圆（例如 30 个圆），作为初始桩位
   - 可以随意分布，不需要均匀

### 执行测试
1. 在 AutoCAD 命令行输入 **C2**（重载插件）
2. 输入 **C1**（打开桩面板）
3. 点击 **桩位优化(已有圆)** 按钮
4. 命令行提示：`请选择一个闭合的多段线:`
   - 点击多段线
5. 命令行提示：`请选择一些圆:`
   - 框选所有圆
   - 按回车确认
6. 命令行提示：`请输入桩直径(单位mm):`
   - 输入 `400`
7. 命令行提示：`请输入目标置换率 (0-100)%:`
   - 输入 `2.2`
8. 等待优化完成（约 10-30 秒）

### 预期结果
- ✅ 图层 `00-HY-桩` 中绘制优化后的圆（桩）
- ✅ 图层 `00-HY-Voronoi` 中绘制 Voronoi 区域
- ✅ 桩位从初始随机分布优化为均匀分布
- ✅ 桩数量与初始圆数量一致
- ✅ 命令行输出：`桩布置完成，共 X 根桩，置换率: X.XX%。`

---

## 常见问题

### Q1: 命令执行后没有反应？
**A**: 检查命令行是否有错误信息。常见原因：
- 未选择闭合多段线
- 多段线包含弧段（暂不支持）
- 输入参数不合法

### Q2: Lloyd 优化耗时过长？
**A**: 当前迭代次数为 4500 次，可以在代码中调整：
```csharp
// PileVoronoiOptimizationCommand.cs, Line 145
pileCenters = _voronoiService.ApplyLloydOptimization(polygon, pileCenters, 4500, diameter);
// 改为更小的值，例如 500
```

### Q3: 圆分组标注没有生成表格？
**A**: 检查是否选择了足够的圆和文字。至少需要 1 个圆和 1 个文字。

### Q4: Voronoi 图绘制不完整？
**A**: 可能是多边形形状过于复杂。建议使用简单的矩形或多边形测试。

---

## 测试检查清单

### 圆分组标注
- [ ] 圆按标高分组到不同图层
- [ ] 图层颜色渐变正确
- [ ] 引线标注显示正确
- [ ] 统计表格生成正确
- [ ] 无文字的圆处理正确

### 桩位 Voronoi 优化
- [ ] 自动生成初始点正确
- [ ] Lloyd 优化收敛
- [ ] Voronoi 图绘制正确
- [ ] 桩圆绘制正确
- [ ] 置换率计算正确

### 桩位优化（已有圆）
- [ ] 选择已有圆正确
- [ ] 过滤多边形内的圆正确
- [ ] 优化后桩位均匀分布
- [ ] 桩数量保持不变

---

**测试完成后，请将结果反馈给开发人员！**
