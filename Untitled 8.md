1. 选择非弯钩段 → 删除该端弯钩（RemoveVertexAt）
          ↓
   射线求交（遍历同图层所有钢筋多段线）→ 找最近交点 （求交点后，射线需要先沿射线方向缩短一个保护层厚度  交点-保护层厚度 ）
          ↓
   计算点 a = 端点 + 方向 × (交点距离 - 保护层厚度)
          ↓
   如果延伸端在起点侧 → ReverseCurve（确保在末尾）
          ↓
   AddVertexAt(末尾, 点a)  → 多段线现在延伸到 a
          ↓
   HookJig(poly, 15d, isVertical=true)
     → 用户实时预览 90° 弯折方向（左/右）
     → 点击确认 → Commit
     → ESC 取消 → 全部回滚（含删除弯钩）

逻辑确认：

- hitPt = 射线与另一根钢筋的交点

- rawDist = 端点到交点的原始距离
- 点a=交点-保护层厚度
- 用户确定方向b
- 沿b方向延伸 ，延伸长度15*d

